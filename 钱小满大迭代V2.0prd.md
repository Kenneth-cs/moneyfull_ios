这份产品迭代规划非常有远见！将“钱小满”从一个传统的记账工具，彻底重构为 **AI Native 的智能财务管家**。

针对您的需求，我为您整理了一份详细的 **PRD（产品需求文档）**。这份文档不仅涵盖了您提到的所有场景，还结合了 iOS 的系统特性（如 Shortcuts、App Intents）给出了技术落地的可行性建议。

---

# 钱小满 (Moneyfull) v2.0 - AI Native 升级 PRD

## 1. 迭代背景与目标
* **背景：** 传统的表单式记账（GUI）存在较高的操作摩擦力，导致用户难以坚持。目前的 AI 功能仅停留在“赋能”阶段（如报表分析）。
* **目标：** 打造“无痛记账”体验。将核心交互路径从“手动输入”升级为“自然语言与自动化（LUI + 自动化）”，实现极简、智能的财务管理。
* **核心指标：** 降低单次记账时长（从 10秒+ 降至 3秒内），提升用户活跃度与记账留存率。

---

## 2. 核心功能模块与交互设计

### 2.1 首页布局重构 (Homepage Revamp)
* **原逻辑：** 底部 TabBar 中间为“+”号，点击进入传统表单记账。
* **新逻辑：**
  1. **传统入口迁移：** 将原有的“+”号（手动记账）上移，融入首页顶部的“支出/收入/结余”数据看板模块中（在卡片收入和储蓄下方，新增一个横向的 `+ 记一笔` 长按钮，参考设计图1），作为传统记账的保留入口。
  2. **AI Native 核心入口：** 底部 TabBar 中间替换为 **“AI 助手”图标**（使用麦克风图标，参考设计图2）。
     * **长按：** 触发【次懒场景：语音记账】。
     * **短按：** 唤起【AI 对话面板】（支持文字输入和历史对话记录）。

### 2.2 次懒场景：AI 语音与对话记账 (Voice & LUI)
* **场景描述：** 走在路上，长按底部 AI 按钮说“买水花了 3 块”。
* **交互流程：**
  1. 长按底部按钮，伴随 Taptic Engine 震动反馈，开始录音。
  2. 松手后，自动进入 **AI 对话页面 (Chat UI)**（参考设计图3，需进行 iOS 原生适配，确保键盘弹出、安全区域、气泡尺寸符合 Apple HIG 规范）。
  3. 屏幕显示用户的语音转文字：“买水花了 3 块”。
  4. **AI 意图识别与追问策略：**
     * **上下文注入：** 每次请求 LLM 时，将用户当前的“分类列表”和“项目列表”作为 Prompt Context 传入，限制 AI 优先在现有分类和项目中映射。
     * **精细化分类识别 (一二级分类)：** 强制要求 AI 识别到二级分类。例如“买咖啡花了25元”，AI 必须识别出一级分类“餐饮”和二级分类“咖啡”。
     * **人机协同创建 (Human-in-the-loop)：** AI 只有“建议权”，没有“直接创建权”。如果 AI 发现现有分类/项目不合适（例如用户没有“咖啡”这个二级分类），它会在卡片上标出 **“✨ 咖啡 (新分类)”** 并归入“餐饮”一级分类下。用户点击“确认”后，系统才真正在底层创建；如果用户修改回老分类，AI 则学习到“不需要此新分类”。
     * **高置信度：** 提取出完整的 JSON（金额、分类、项目、备注），直接生成一张 **“记账确认卡片”**。
     * **低置信度/语义模糊：** 如果用户说“花了 50”（没说干嘛），AI 不盲目猜测，而是回复：“这 50 元是花在什么地方了呢？”引导用户补充。
  5. **自由修改与记忆纠错：** 卡片上的元素支持直接点击修改。如果 AI 将“瑞幸”分到了“餐饮”，用户手动改为“咖啡”，AI Agent 会将此规则写入长期记忆。
  6. **自动保存：** 倒计时 3 秒无操作，或用户点击“确认”，卡片状态变为“已入账”。

### 2.3 最懒场景：视觉记账 (Screenshot/OCR)
* **场景描述：** 刚用微信/支付宝付完款，双击手机背面，自动完成记账。
* **实现方案（结合 iOS 特性）：**
  1. **配置引导：** 在 App 设置中引导用户配置 iOS 的“轻点背面 (Back Tap)”功能，绑定一个专属的“钱小满记账”快捷指令 (Shortcut)。
  2. **快捷指令逻辑：** 获取当前屏幕截图 -> 传递给钱小满 App。
  3. **App 内处理（混合解析方案）：** 
     * **方案 A（端侧 OCR + 云端 LLM，推荐）：** 先使用 iOS 原生的 `Vision` 框架（完全免费、极速、保护隐私）在端侧提取截图中的所有文字。然后将提取出的纯文本 + 用户的分类列表，发给轻量级 LLM（如 DeepSeek-Chat 或千问 Turbo）提取结构化数据。这对于标准的微信/支付宝支付截图准确率极高，且 Token 成本极低。
     * **方案 B（云端 VLM）：** 如果是复杂的纸质小票照片，排版错乱导致 OCR 文本丢失逻辑关系，则降级调用云端多模态大模型（如 Qwen-VL 或 GPT-4o）直接“看图”提取。
  4. **用户反馈：** 提取成功后，App 发送一条本地推送 (Local Notification)：“💸 成功记入支出 25 元（星巴克）”。用户无需打开 App 即可完成，只需偶尔看报表。

### 2.4 自动化流：被动记账与周期账单
* **关于支付通知的澄清与方案：**
  * *技术限制：* iOS 沙盒机制严格，第三方 App **绝对无法**直接读取微信/支付宝的 Push 通知。
  * *替代方案 A（短信自动化）：* 针对绑定了银行卡短信提醒的用户，利用 iOS“快捷指令-自动化-收到信息”，当收到特定银行短信时，自动触发钱小满的 App Intent 写入数据。
  * *替代方案 B（微信/支付宝账单截图）：* 鼓励用户在月底批量截图账单列表，钱小满支持“批量导入图片”，AI 一次性解析 10 条账单并入库。
* **周期性账单 (Recurring Bills)：**
  * 新增“周期账单”管理。用户可设置房租、iCloud 订阅等。
  * 每月到期日，系统自动生成账单，并发送推送：“已自动为您记入本月房租 3000 元，点击修改”。

### 2.5 传统 GUI 极致优化 (肌肉记忆流)
针对依然习惯手动记账的老用户，或在不方便说话的场景，保留并优化传统入口：

* **场景化智能默认值（非破坏性排序）：**
  * **痛点解决：** 频繁改变分类网格的顺序会破坏用户的“肌肉记忆”，导致找不到分类。
  * **优化方案：** 保持原有的分类网格顺序**绝对固定**。在网格最上方新增一行专属的 **“✨ 智能推荐”** 区域（仅展示 3 个 Icon）。
  * 早上 8:00 打开面板，推荐区显示“早餐、交通”。周末打开，推荐区显示“娱乐、购物”。用户既可以享受智能推荐的快捷，又能在下方固定网格中通过肌肉记忆找到其他分类。

### 2.6 打破 App 边界 (iOS Ecosystem)
* **锁屏小组件 (Lock Screen Widgets)：** 提供一个一键录音的小组件。锁屏状态下点一下，直接开始录音记账。
* **Siri 深度整合 (App Intents)：** 支持用户直接对手机说：“嘿 Siri，用钱小满记一笔，打车 50 块”。

### 2.7 AI Agent 集群与个性化记忆 (Agentic Architecture)
* **核心理念：** 钱小满的 AI 不是一个简单的“文本解析器”，而是一个有记忆、有工具调用能力 (Function Calling) 的 Agent 集群。
* **主 Agent (Persona)：** 设定为“小满”-用户的专属财务管家。负责意图路由（判断用户是想记账、查账、还是闲聊）。
* **分层上下文记忆 (Context & Memory Management)：** 解决长期对话导致 Token 爆炸的问题。
  * **短期记忆（滑动窗口）：** 每次请求 LLM 时，只携带**最近 5 条**的真实对话历史（预制消息不计入），用于解决多轮追问（如 AI 问“花在哪了”，用户回“打车”）。超过 5 条的对话不占用 Token。
  * **长期记忆（规则向量化）：** 用户的历史纠错绝对**不以原始对话保存**。当用户在 UI 上纠错（如把 AI 识别的“餐饮”改成了“咖啡”）时，系统在本地生成一条精简的 JSON 规则：`{"keyword": "瑞幸", "category": "咖啡", "weight": 1}`。每次请求 LLM 时，只把这些精简规则注入 System Prompt，实现“越用越懂你”且 Token 消耗极低。
  * **画像记忆（Onboarding）：** 用户完成测评后，系统生成画像 Prompt（包含用户画像、收入类型、记账习惯、健康分、JTBD），存储在 `UserDefaults["aiPersonaSystemPrompt"]`，每次 API 调用都作为 System Prompt 的一部分注入，使 AI 始终了解用户背景。
  * **预制消息过滤：** Onboarding 阶段的预制消息（`isPrescripted=true`）不进入 API 上下文，避免重复和 Token 浪费。
* **工具调用 (Tools)：** AI 拥有调用 App 内部方法的权限，如 `record_transaction()`（记一笔）、`query_balance()`（查当月结余）、`update_budget()`（改预算）。

---

## 3. 技术可行性与架构建议

1. **AI 引擎选择：**
   * **语音转文字：** 使用 iOS 原生的 `SFSpeechRecognizer`（免费、极速、端侧）或调用 Whisper API。
   * **意图与实体提取 (LLM)：** 建议初期接入如 DeepSeek-Chat、通义千问或 GPT-4o-mini 等高性价比 API。将用户的自然语言转化为 JSON 格式（包含 amount, category, type, note）。
   * **图片解析 (VLM)：** 接入支持 Vision 的大模型（如 GPT-4o 或 Qwen-VL）来提取截图中的账单信息。
2. **iOS 快捷指令与 App Intents：**
   * 需要在 Swift 代码中引入 `AppIntents` 框架，暴露 `RecordTransactionIntent` 给系统，这样快捷指令和 Siri 才能在后台直接向数据库写入数据。
3. **端侧数据存储架构（Privacy First & 无服务器成本）：**
   * **无需 MySQL：** 遵循苹果“端侧智能”哲学，Agent 的记忆和聊天记录完全存在本地，通过 iCloud 同步，保护隐私且无服务器成本。
   * **基础数据兼容：** 现有的 SwiftData 模型（`Transaction`, `Project`, `Category`）完全复用，只需在 `Transaction` 增加 `source` 字段 (manual/voice/image/auto)。
   * **新增 Agent 模型：** 在 SwiftData 中新增 `ChatHistory`（存最近几百条对话）和 `MemoryRule`（存用户的纠错规则 JSON）。

---

## 5. Prompt 设计与数据可见性

### 5.1 System Prompt 设计框架
AI 的表现极大程度上取决于 Prompt 的设计。钱小满的 System Prompt 结构如下：
1. **Role (人设):** "你是用户的专属财务管家'小满'，语气专业、温暖、简洁。"
2. **Task (任务):** "从用户的自然语言中提取记账信息，并严格按照 JSON 格式输出。"
3. **Constraints (约束):** 
   * "必须精细化识别到一级分类 (groupName) 和二级分类 (name)。"
   * "优先使用 Context 中提供的现有分类。如果用户提到的二级分类不存在，但属于合理的高频场景，请在返回的 JSON 中标记 `is_new_category: true`，并为其分配合适的一级分类。"
   * "如果用户表达模糊，不要猜测，输出 `{"status": "need_clarification", "reply": "追问话术"}`。"
4. **Context (动态注入):**
   * `Available Categories: [{"group": "餐饮", "name": "正餐"}, {"group": "交通", "name": "打车"}...]`
   * `Available Projects: [日常, 装修...]`
   * `User Memory Rules: [{"keyword": "瑞幸", "category": "咖啡"}]`

### 5.2 用户对 AI 记忆的掌控权 (Data Visibility)
为了建立信任，AI 的“记忆”对用户必须是**完全透明且可控**的。
* **记忆管理页面：** 在“我的 (Profile)”页面新增“AI 记忆管理”入口。
* **功能：** 用户可以清晰地看到 AI 学习到的所有规则（例如：列表显示“当我说'瑞幸'时，记入'餐饮'”）。用户可以随时删除或修改这些规则。
* **对话历史：** Chat UI 中的历史对话可以随时清空。

## 6. 迭代实施路径 (Roadmap)

为了控制开发风险，建议分三个阶段（Phase）进行。详细的开发拆解请参考独立的开发规划文档：`.cursor/plans/钱小满V2.0开发规划.md`。

### 进度更新

| 阶段 | 状态 | 完成时间 |
|------|------|----------|
| 阶段一：核心架构与 AI 基础体验 | ✅ 已完成 | 2026-05-20 |
| 阶段二：视觉解析与系统级整合 | ✅ 已完成 | 2026-05-21 |
| 阶段三：传统 GUI 优化与自动化 | ✅ 已完成 | 2026-05-21 |

---

## 7. 阶段三完成说明 (2026-05-21)

### 已完成功能

1. **智能推荐分类** - 根据时间/场景动态推荐 3 个常用分类
2. **键盘交互优化** - 滑动页面自动收起自定义键盘
3. **周期账单模型** - 新增 `RecurringBill` 数据模型，支持 daily/weekly/monthly/yearly
4. **AI 记忆管理** - 用户可查看/删除 AI 学习到的纠错规则
5. **LLM 财务建议** - 深度报告按钮接入 AI 分析
6. **语音记账** - 长按底部 AI 按钮录音，松手自动识别
7. **快捷指令 OCR 记账** - 截屏 → 提取文本 → 打开 App 自动识别
8. **日期格式优化** - 纯数字格式，修复昨天显示今天的问题

### 快捷指令方案

最终采用 **App Intent + URL Scheme 桥梁方案**：
- 快捷指令配置：截屏 → 提取文本 → 钱小满（内容：图像中的文本）
- Intent 内部调用 `openURL` 唤起 App，不碰数据库，彻底解决 CloudKit 崩溃
- App 被唤起后通过 `.onOpenURL` 处理，弹出 AI 对话页

---

## 8. 订阅管理与兑换码修复 (2026-07-01)

### 修复内容

#### 8.1 已订阅用户订阅页入口

**问题**：已订阅用户在个人中心没有进入 PaywallView 的入口，无法查看当前订阅详情或升级到更高档位方案。

**解决方案**：
- `StoreManager` 新增 `currentPlanTitle`、`currentPlanExpiry`、`currentProductID` 属性
- 在 `verifyCurrentEntitlements()` 中解析 `transaction.productID` 和 `transaction.expirationDate` 填充上述属性
- `PremiumStatusRow` 新增可选 `onViewPlans` 回调，在"管理"按钮旁添加"查看方案"按钮
- ProfileView 中已订阅区块传入 `onViewPlans: { showPaywall = true }`，点击即可进入 PaywallView
- 硬编码的 `planTitle: "年卡"` 和 `expiryText: "已激活"` 替换为 `storeManager.currentPlanTitle` 和 `storeManager.currentPlanExpiry`

#### 8.2 PaywallView 已订阅用户体验优化

**问题**：已订阅用户打开 PaywallView 时，`onChange(of: storeManager.isPremium)` 会立即 dismiss 页面。

**解决方案**：
- 新增 `wasPremiumOnOpen` 状态变量，在 `onAppear` 时记录初始 `isPremium` 状态
- `onChange` 仅在 `isPremium` 从 false 变为 true 时 dismiss（即新购买后），已订阅用户打开时保持页面
- 方案卡片通过 `isCurrentPlan` 参数显示「当前方案」角标（香槟金色），便于用户识别当前方案

#### 8.3 兑换码入口可见性

**问题**：`RedeemCodeRow` 放在 `if storeManager.isPremium` 块内，非会员完全看不到兑换码入口。

**解决方案**：
- 将 `RedeemCodeRow` 同时添加到非会员区块，在 `PremiumUpgradeBanner` 下方独立展示
- 非会员区块包含升级横幅 + 兑换码入口，形成完整的付费引导

#### 8.4 兑换码弹窗静默失败修复

**问题**：`presentOfferCodeSheet()` 使用 `UIApplication.shared.connectedScenes.first` 查找 scene，在 `fullScreenCover` 弹出层内可能找不到正确的 foreground scene 导致静默失败。

**解决方案**：
- 改用 `foregroundActive` 状态过滤：`UIApplication.shared.connectedScenes.first(where: { $0.activationState == .foregroundActive })`
- 通过 `@MainActor` 确保在主线程执行，避免线程安全问题

### 涉及文件

| 文件 | 修改内容 |
|------|---------|
| `StoreManager.swift` | 新增 `currentPlanTitle/currentPlanExpiry/currentProductID` 属性；修复 `presentOfferCodeSheet()` |
| `PaywallView.swift` | `PremiumStatusRow` 新增 `onViewPlans` 回调；`PaywallPlansSection` 新增 `currentProductID` 参数；方案卡片新增「当前方案」角标；`wasPremiumOnOpen` 状态追踪 |
| `ProfileView.swift` | 已订阅区块使用动态订阅信息 + `onViewPlans`；非会员区块新增兑换码入口 |

---

## 9. 个人中心 & 订阅页 UI 优化 (2026-07-01)

### 9.1 订阅页升/降级 UX 优化

**问题**：PaywallView 的 CTA 按钮未区分升级和降级，年卡用户点击月卡显示"开通月卡"可点击，产生误导；当前方案卡片用金色高亮，视觉权重高于推荐升级方案，不利于转化。

**解决方案**：

- `PaywallCTASection` 新增 `planLevel()` 函数（终身=3 > 年卡=2 > 月卡=1）
- 新增 `isDowngrade` 计算属性：选中方案级别 < 当前方案级别
- `ctaTitle` 区分三种状态：
  - 降级 → "如需降级请前往订阅管理"（按钮置灰禁用）
  - 升级 → "升级到年卡 ¥XX" / "买断终身 ¥XX"
  - 新开通 → "开通月卡 ¥XX"
- 升级时按钮下方显示"· 立即生效，按比例计算差价 ·"提示文案
- 降级时按钮样式变为灰色背景，`.disabled(isDowngrade)`
- **当前方案徽章**：从香槟金改为灰色低调样式（`Color.gray.opacity(0.15)`），让视觉重心落在升级目标方案上
- `PaywallPlanCard` / `PaywallPlanCardWide` 新增 `isDimmed` 参数，当前方案卡片 `opacity(0.5)` 淡化

**转化逻辑**：月卡用户打开订阅页时，月卡灰暗退后，年卡绿色高亮且默认选中，用户视线自然引导至升级方案。

### 9.2 个人中心功能菜单网格化重构

**问题**：功能菜单为单列长列表（11 行），视觉拥挤，需要大量滚动，且图标圆形样式与竞品差距明显。

**解决方案**：

- 废弃单列 `MenuItem` 长列表布局
- 新增 `MenuGridItem` 组件（方形圆角图标 + 底部文字，全区域可点击 `contentShape(Rectangle())`）
- 菜单拆分为两张卡片：
  - **常用操作卡**（7格，4列网格）：无痛记账、月度预算、分类管理、项目排序、AI 记忆、主题设置、记账提醒
  - **数据·其他卡**（4格，4列网格，一行）：导出数据、导入账单、帮助反馈、好评支持
  - **iCloud 状态行**（全宽独立卡，保留动态内容展示）

**文案精简**（同步更新）：

| 原文案 | 新文案 | 说明 |
|--------|--------|------|
| 无疼记账设置 | 无痛记账 | 改「疼」为「痛」，去「设置」后缀 |
| 月度预算设置 | 月度预算 | 去「设置」后缀 |
| AI 记忆管理 | AI 记忆 | 精简 |
| 主题设置（跟随系统）| 主题设置 | 去动态状态后缀（状态在点击后页面内展示）|
| 帮助与反馈 | 帮助反馈 | 精简 |
| 给个好评 ❤️ | 好评支持 | 去 emoji，统一风格 |

### 9.3 PremiumUpgradeBanner 对齐修复

**问题**：`PremiumUpgradeBanner` 组件自身带 `.padding(.horizontal, 24)`，父容器 `ProfileView` 也有 `.padding(.horizontal, 24)`，双重叠加导致横幅比下方卡片窄了 48pt，左右不对齐。

**解决方案**：移除 `PremiumUpgradeBanner` 内部的 `.padding(.horizontal, 24)`，由父级统一控制边距。

### 涉及文件

| 文件 | 修改内容 |
|------|---------|
| `PaywallView.swift` | CTA 升/降级逻辑；`planLevel()`；`isDowngrade`；`isDimmed` 参数；当前方案徽章灰化；移除 `PremiumUpgradeBanner` 多余 padding |
| `ProfileView.swift` | 菜单区块替换为两张网格卡 + iCloud 全宽行；文案精简 |
| `ProfileView.swift` | 新增 `MenuGridItem` 结构体 |

---

## 10. 新用户欢迎页重构 (2026-07-11)

### 10.1 背景与目标

原有的 `OnboardingView` 为三页滑动引导页（文字 + 插图），页面信息密度低且与测评流程割裂（看完引导→回首页→再弹测评）。本次替换为一张全屏设计稿欢迎页，视觉冲击力更强，并将欢迎→测评打通为连续体验。

### 10.2 产品设计决策

| 决策点 | 旧方案 | 新方案 |
|--------|--------|--------|
| 欢迎页形态 | 3 页滑动引导（SwiftUI 纯代码） | 1 张全屏设计稿（PNG 背景 + SwiftUI 悬浮按钮） |
| 进入路径 | App 启动 → 首页 → 弹引导层 | App 启动 → 直接显示欢迎页（无首页背景） |
| 测评衔接 | 引导看完 → 回首页 → 再弹测评 | 点击按钮 → 直接进测评（无中间跳转） |
| 通知权限 | App 初始化时立即弹窗请求 | 移除启动时弹窗，由用户在「提醒设置」页主动开启 |

### 10.3 启动流程

```
App 启动
  ↓ 首次用户（hasSeenOnboarding = false）
WelcomeView（全屏欢迎页）
  ↓ 点击「一键领取专属方案」
  · 标记 hasSeenOnboarding = true
  · 动画过渡
AssessmentView（测评答题）
  ↓
personaLoadingView（1.8 秒画像生成动画）
  ↓
PersonaResultView（画像结果展示）
  ↓ 完成
MainTabView（首页）+ 触发 AI 引导对话
```

### 10.4 WelcomeView 技术实现

**背景图**：`welcome_bg.png`，1290 × 2796 px（@3x），存入 `Assets.xcassets/welcome_bg.imageset`（Universal / Single Scale: @3x）。SwiftUI 以 `scaledToFill` 全屏铺满，`GeometryReader` 精确适配各机型宽高。

**悬浮按钮**：纯 SwiftUI，`ZStack(alignment: .bottom)` 叠加在背景图上。按钮为深绿色渐变胶囊（`#5BAF8A → #2C6957`），`PressableButtonStyle` 实现按压弹性（`scaleEffect 1.0 → 0.96`，`spring` 动画），点击触发 `UIImpactFeedbackGenerator(.medium)` 震动反馈。

**底部间距**：读取 `UIWindowScene.safeAreaInsets.bottom` 动态计算，确保 iPhone SE（无底部安全区）和 Dynamic Island 机型视觉效果一致。

**副文案**：「30秒即可完成  ·  完全免费」由 SwiftUI Text 渲染，不内嵌在背景图中，便于后续多语言和文案调整。

### 10.5 涉及文件

| 文件 | 修改内容 |
|------|---------|
| `Views/OnboardingView.swift` | 原 `OnboardingView`（3页引导）完全替换为 `WelcomeView`（全屏欢迎页 + 悬浮按钮）；移除冗余的 `buttonScale` 状态；底部间距改为 safeAreaInsets 自适应 |
| `ContentView.swift` | 首次启动流程重构：`showWelcome / showAssessment` 双态控制；`firstLaunchFlow` 替代整个 MainTabView 先行渲染；欢迎页→测评一体化衔接 |
| `moneyfull_iosApp.swift` | 移除 `App.init()` 中的 `NotificationManager.shared.requestPermission()` 调用，不再在启动时弹通知权限弹窗 |
| `Assets.xcassets/welcome_bg.imageset` | 新增 `welcome_bg.png`（1290×2796 px @3x）|

---

## 11. 项目类型智能配置系统 (2026-07-13)

### 11.1 背景与目标

**问题**：原有的"大件 vs 日常"分析功能存在硬编码问题：
- 大件关键词只匹配旅行场景的交通/住宿（如机票、酒店）
- 文案和对照标准写死为旅行场景（如"特种兵穷游"、"¥800/天 = 贵妇级别"）
- 对于其他类型的项目（如新房装修、婚礼、买车等），分析结果不准确且文案不匹配

**目标**：实现项目类型智能配置系统，根据项目名称/描述自动识别项目类型，动态返回对应的大件关键词、文案和对照标准，覆盖31个常见生活场景。

### 11.2 项目类型覆盖（31个场景）

| 类别 | 项目类型 | 识别关键词 | 大件分类 | 对照标准 |
|------|---------|-----------|---------|---------|
| **旅行出行** | 旅行/旅游 | 旅行、旅游、度假 | 交通+住宿 | ¥800/天 = 贵妇级别 |
| | 出差 | 出差、公务、商务 | 交通+住宿 | ¥500/天 = 商务标准 |
| | 自驾游 | 自驾、公路旅行 | 油费+住宿 | ¥600/天 = 豪华自驾 |
| | 露营 | 露营、野营、户外 | 装备采购 | ¥500/次 = 豪华露营 |
| | 滑雪 | 滑雪、冬运、雪场 | 雪票+装备 | ¥1000/天 = 高端滑雪 |
| **家居装修** | 新房装修 | 装修、新房、毛坯 | 家具+家电 | ¥2000/㎡ = 豪装标准 |
| | 旧房翻新 | 翻新、改造、老房 | 家具+建材 | ¥5000/项 = 全面翻新 |
| | 搬家 | 搬家、乔迁 | 搬家费+新家具 | ¥3000/次 = 品质搬家 |
| | 家具采购 | 家具、沙发、床 | 主要家具 | ¥2000/件 = 品质家居 |
| | 家电采购 | 家电、电器、空调 | 主要家电 | ¥3000/件 = 智能家电 |
| **婚礼庆典** | 婚礼 | 婚礼、结婚、婚庆 | 婚庆+酒店 | ¥100000/场 = 豪华婚礼 |
| | 满月酒 | 满月、百日、周岁 | 酒席+布置 | ¥20000/场 = 隆重举办 |
| | 生日派对 | 生日、派对 | 场地+蛋糕 | ¥5000/场 = 盛大派对 |
| | 毕业典礼 | 毕业、毕业季 | 学士服+摄影 | ¥1000/人 = 精彩记录 |
| **教育学习** | 留学 | 留学、出国、海外 | 学费+住宿 | ¥10000/月 = 品质留学 |
| | 考试备考 | 考研、考公、考编 | 课程+教材 | ¥3000/月 = 高端辅导 |
| | 培训学习 | 培训、课程、技能 | 学费+教材 | ¥5000/期 = 高端培训 |
| | 驾照学习 | 驾照、驾校、学车 | 学费+考试费 | ¥8000/次 = VIP班 |
| **健康医疗** | 体检医疗 | 体检、医疗、看病 | 检查+治疗 | ¥5000/次 = 高端医疗 |
| | 牙齿矫正 | 牙齿、矫正、正畸 | 矫正+种植 | ¥30000/次 = 隐形矫正 |
| | 医美 | 医美、美容、整形 | 手术+注射 | ¥10000/次 = 高端医美 |
| | 健身 | 健身、运动、减肥 | 年卡+私教 | ¥2000/月 = 私教健身 |
| **宠物** | 宠物 | 宠物、猫、狗 | 医疗+粮食 | ¥1000/月 = 品质养宠 |
| **汽车** | 买车 | 买车、购车、提车 | 车款+税费 | ¥200000/辆 = 豪华座驾 |
| | 汽车保养 | 保养、维修、修车 | 机油+轮胎 | ¥2000/次 = 深度保养 |
| **数码电子** | 电脑组装 | 电脑、组装、装机 | 核心硬件 | ¥10000/台 = 旗舰配置 |
| **投资理财** | 投资理财 | 投资、理财、股票 | 本金+大额 | ¥10000/笔 = 高端投资 |
| **节日购物** | 年货采购 | 年货、春节、过年 | 礼品+食材 | ¥5000/户 = 丰盛年货 |
| | 购物节 | 双十一、618、大促 | 数码+家电 | ¥500/件 = 疯狂购物 |
| **社交** | 社交聚会 | 聚会、聚餐、团建 | 场地+餐饮 | ¥500/次 = 精致聚会 |
| **默认** | 日常开销 | 日常、生活 | 交通+住宿 | ¥300/天 = 品质生活 |

### 11.3 技术实现

**核心文件**：`Models/ProjectTypeConfig.swift`

```swift
// 项目类型枚举
enum ProjectType: String, CaseIterable, Identifiable {
    case travel, businessTrip, roadTrip, camping, skiing
    case renovation, remodeling, moving, furniture, appliance
    case wedding, babyShower, birthday, graduation
    case studyAbroad, examPrep, training, drivingSchool
    case medical, dental, cosmetic, fitness, pet
    case carPurchase, carMaintenance, computerBuild
    case investment, newYearShopping, shoppingFestival
    case social, daily
}

// 项目类型配置结构
struct ProjectTypeConfig {
    let type: ProjectType
    let displayName: String        // 显示名称
    let icon: String              // SF Symbol
    let keywords: [String]        // 识别关键词
    let bigItemKeywords: [String] // 大件分类关键词
    let bigItemLabel: String      // 大件显示文案
    let dailyLabel: String        // 日常显示文案
    let description: String       // 功能描述
    let referenceStandard: ReferenceStandard // 对照标准
}

// 对照标准
struct ReferenceStandard {
    let lowLabel: String          // 低端标签
    let highLabel: String         // 高端标签
    let unit: String              // 单位
    let threshold: Double         // 高端阈值
    let format: (Double) -> String // 格式化函数
}
```

**智能识别逻辑**：
```swift
// 按优先级匹配（越具体的越靠前）
let priorityOrder: [ProjectType] = [
    .wedding, .babyShower, .graduation, .studyAbroad,
    .renovation, .remodeling, .skiing, .camping,
    .roadTrip, .businessTrip, .travel,
    // ... 其他类型
    .social, .daily  // 默认
]

// 遍历优先级列表，匹配关键词
for type in priorityOrder {
    let matched = config.keywords.filter { text.contains($0) }
    if !matched.isEmpty { return type }
}
return .daily  // 默认返回日常开销
```

**使用方式**（在 `ProjectLifestyleView` 中）：
```swift
// 获取项目类型配置
let config = ProjectTypeConfigManager.shared.getConfig(
    name: project.name,
    description: project.desc
)

// 使用动态配置
Text(config.bigItemLabel)  // "大件（家具+家电）"
Text(config.description)   // "剔除家具家电，看建材辅材和人工费花了多少"
Text(config.referenceStandard.format(dailyFree))  // "¥2000/㎡ = 豪装标准"
```

### 11.4 涉及文件

| 文件 | 修改内容 |
|------|---------|
| `Models/ProjectTypeConfig.swift` | 新增项目类型智能配置系统，包含31个项目类型的完整配置（关键词、大件分类、文案、对照标准）|
| `Views/ProjectLifestyleView.swift` | 集成智能配置系统，大件识别使用动态关键词，文案和对照标准根据项目类型动态显示 |