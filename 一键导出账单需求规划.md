好，现在我对整个项目有了清晰认识。来系统分析这个需求。

---

## 我的判断：这是个 **必做但不急做** 的功能

### 先看现状

目前 `ProfileView.swift` 里已经有"导出数据"入口，但实现是：

```190:194:moneyfull_ios/Views/ProfileView.swift
        .alert("导出数据", isPresented: $showExportAlert) {
            Button("好的", role: .cancel) {}
        } message: {
            Text("数据导出功能正在开发中，敬请期待～")
        }
```

也就是说，**壳子有了，里面是空的**。这个坑需要填，但填之前值得想清楚填成什么样。

---

## 三类需求的优先级分析

### 需求 1：主流记账 App 迁移（刚需 ✅）

用户从钱小满迁移到随手记、MoneyMoney、记账本等，或反向迁入。这是**真刚需**，理由：

- 用户数据是用户的，拿不走=数据绑架，口碑杀手
- 各大记账 App 基本都支持 CSV 导入，格式较通用
- 这是用户**留存**的信任背书：我走得掉，所以我敢留下来

**结论：必须做，优先级高。**



## 数据模型评估

当前 `Transaction` 模型的字段：

```101:144:moneyfull_ios/Models/Models.swift
@Model
final class Transaction {
    var id: UUID = UUID()
    var amount: Double = 0
    var rawType: String = TransactionType.expense.rawValue
    var rawSource: String = TransactionSource.manual.rawValue
    var categoryName: String = ""
    var categoryIcon: String = ""
    var categoryColorHex: String = "#A8E6CF"
    var note: String = ""
    var date: Date = Date()
    var createdAt: Date = Date()
    var project: Project? = nil
```

**好消息**：字段已经足够支撑 CSV 导出，不需要改模型。

导出列设计建议：

| CSV 列名 | 来源字段 | 备注 |
|---------|---------|------|
| 日期 | `date` | `yyyy-MM-dd` |
| 时间 | `date` | `HH:mm` |
| 类型 | `type` | 支出/收入 |
| 金额 | `amount` | 正数，不带符号 |
| 一级分类 | `categoryName`（groupName 需回查 Category） | 或直接用 categoryName |
| 分类 | `categoryName` | |
| 项目 | `project?.name` | 无项目填"日常" |
| 备注 | `note` | |
| 记录方式 | `source` | 手动/语音/图片 |

---

## 规划建议（分两期）

### Phase 1（V2.1 最快两天能做完）：基础 CSV 导出

**功能范围：**
- 导出全部数据，或按**时间范围**筛选（本月/本年/全部/自定义）
- 纯 CSV 格式（`.csv`），UTF-8-BOM 编码（兼容 Excel 直接打开中文）
- 通过系统分享面板（`UIActivityViewController`）导出，支持：保存到文件 App、发邮件、发给微信/钉钉
- 入口：个人中心"导出数据"（现有入口替换 alert）→ Sheet 弹出筛选选项

**不需要引入任何第三方库，纯 Swift 字符串拼接即可。**

技术要点：
```swift
// UTF-8 BOM 处理，让 Excel 正确识别中文
let bom = "\u{FEFF}"
var csv = bom + "日期,时间,类型,金额,分类,项目,备注,记录方式\n"
```

### Phase 2（V3.0 按需做）：增强版

- Excel（`.xlsx`）格式：可考虑引入 [CoreXLSX](https://github.com/CoreOffice/CoreXLSX)（读）+ [xlsxwriter-ios](https://github.com/jmcnamara/XlsxWriter-iOS) 或用 HTML → 分享技巧生成
- 分 Sheet 导出：Sheet1 全部流水，Sheet2 按项目汇总，Sheet3 按分类汇总
- 导入功能（从其他 App 迁入）：解析通用 CSV 格式

**但坦白说，Phase 2 的 Excel 在 iOS 上实现相对麻烦**，不引入三方库就只能生成 `.csv`，用户在 Mac 上用 Excel/Numbers 打开完全没问题。大多数用户对"CSV 还是 xlsx"无感，只要能打开、数据对就行。

---

## 一个关键体验细节

不要做成"点击 → 直接弹系统分享"，而是要做一个**导出配置 Sheet**：

```
┌────────────────────────────┐
│  📤 导出账单               │
│                            │
│  时间范围                  │
│  ○ 本月   ● 本年   ○ 全部  │
│  ○ 自定义区间              │
│                            │
│  导出范围                  │
│  ✓ 全部项目                │
│  □ 仅选定项目              │
│                            │
│  格式：CSV（.csv）         │
│                            │
│  [  取消  ]  [ 导出 📤 ]   │
└────────────────────────────┘
```

这样用户有控制感，也减少"导出了一堆不需要的数据"的吐槽。

---

## 总结

| 功能 | 优先级 | 复杂度 | 建议 |
|-----|--------|--------|------|
| CSV 全量/月度导出 | 🔴 高 | 低 | V2.1 做，2天搞定 |
| 导出筛选器 UI | 🟠 中 | 低 | 同期做 |
| Excel(.xlsx) 格式 | 🟡 低 | 高 | V3.0 再考虑 |
| CSV 导入（从其他 App 迁入） | 🟠 中 | 中 | V3.0 |

**一句话：Phase 1 的 CSV 导出，用纯 Swift 就能实现，没有依赖风险，覆盖 90% 场景，建议作为下一个迭代优先项。** 