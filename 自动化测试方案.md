# 钱小满商业化落地 · 自动化测试方案

> 版本：V1.0 | 日期：2026-06-25

---

## 一、测试策略总览

### 1.1 测试金字塔

```
         ┌─────────────────┐
         │   UI 测试 (10%)   │  ← Paywall 流程、端到端场景
         ├─────────────────┤
         │  集成测试 (30%)   │  ← StoreKit 模拟、订阅状态联动
         ├─────────────────┤
         │  单元测试 (60%)   │  ← 业务逻辑、数据模型、限流算法
         └─────────────────┘
```

### 1.2 测试框架选型

| 层级 | 框架 | 用途 |
|------|------|------|
| 单元测试 | XCTest + Swift Testing | 业务逻辑验证 |
| 集成测试 | XCTest + StoreKit Test Environment | StoreKit 购买流程模拟 |
| UI 测试 | XCUITest | 关键用户流程自动化 |
| 代码覆盖率 | Xcode 内置 Coverage | 目标 ≥ 80%（商业化模块） |

---

## 二、测试环境配置

### 2.1 StoreKit 测试环境

**配置文件：`StoreKit/StoreKitConfiguration.storekit`**

```json
{
  "identifier": "moneyfull_test",
  "products": [
    {
      "id": "com.moneyfull.premium.monthly",
      "type": "auto-renewable",
      "displayName": "月卡",
      "price": "9.9",
      "subscriptionGroupID": "premium"
    },
    {
      "id": "com.moneyfull.premium.annual",
      "type": "auto-renewable",
      "displayName": "年卡",
      "price": "68",
      "subscriptionGroupID": "premium"
    },
    {
      "id": "com.moneyfull.premium.lifetime",
      "type": "non-consumable",
      "displayName": "终身",
      "price": "198"
    },
    {
      "id": "com.moneyfull.fuelpack.200",
      "type": "consumable",
      "displayName": "燃料包200次",
      "price": "6"
    },
    {
      "id": "com.moneyfull.fuelpack.500",
      "type": "consumable",
      "displayName": "燃料包500次",
      "price": "12"
    }
  ],
  "subscriptionGroups": [
    {
      "id": "premium",
      "displayName": "Premium 订阅"
    }
  ]
}
```

**Xcode 配置：**
1. 项目 → Target → Signing & Capabilities → In-App Purchase
2. Scheme → Edit Scheme → Run → Options → StoreKit Configuration → 选择上述文件

### 2.2 Mock 依赖注入

```swift
// TestHelpers/MockStoreManager.swift
class MockStoreManager: ObservableObject {
    @Published var isPremium: Bool = false
    @Published var fuelCredits: Int = 0
    
    func setPremium(_ value: Bool) { isPremium = value }
    func addFuelCredits(_ amount: Int) { fuelCredits += amount }
    func consumeFuelCredit() { fuelCredits = max(0, fuelCredits - 1) }
}

// TestHelpers/MockLLMService.swift
class MockLLMService: LLMServiceProtocol {
    var mockResponse: Result<TransactionParseResult, Error> = .success(...)
    var callCount: Int = 0
    
    func parseTransaction(from text: String, context: String) async throws -> TransactionParseResult {
        callCount += 1
        return try mockResponse.get()
    }
}
```

---

## 三、单元测试用例设计

### 3.1 StoreManager 测试（Phase 1）

**文件：`moneyfull_iosTests/StoreManagerTests.swift`**

| 测试 ID | 测试场景 | 输入/前置条件 | 预期结果 | 优先级 |
|---------|---------|--------------|---------|--------|
| SM-001 | 初始化状态 | App 冷启动 | `isPremium = false`, `fuelCredits = 0` | P0 |
| SM-002 | 加载产品列表 | 网络正常 | 返回 5 个产品，价格正确 | P0 |
| SM-003 | 加载产品失败 | 网络异常 | 抛出错误，不影响 App 启动 | P1 |
| SM-004 | 购买月卡成功 | 用户确认购买 | `isPremium = true`，`UserDefaults` 持久化 | P0 |
| SM-005 | 购买失败-用户取消 | 用户取消弹窗 | 状态不变，无副作用 | P1 |
| SM-006 | 购买失败-支付失败 | 支付网络错误 | 抛出错误，提示用户重试 | P1 |
| SM-007 | 恢复购买成功 | 有历史订阅 | `isPremium = true` | P0 |
| SM-008 | 恢复购买-无历史 | 新设备无购买记录 | 状态不变，不报错 | P1 |
| SM-009 | 订阅过期 | 订阅到期后重启 App | `isPremium = false` | P0 |
| SM-010 | 燃料包购买 | 购买 200 次燃料包 | `fuelCredits += 200`，`UserDefaults` 持久化 | P0 |
| SM-011 | 燃料包消耗 | 调用 `consumeFuelCredit()` | `fuelCredits -= 1`，到 0 停止 | P0 |
| SM-012 | 燃料包跨日持久化 | 购买后重启 App | `fuelCredits` 保持不变 | P0 |
| SM-013 | 兑换码入口 | 调用 `presentOfferCodeSheet()` | 弹出系统兑换界面 | P2 |
| SM-014 | 并发购买防护 | 快速连续点击购买 | 仅触发一次购买流程 | P1 |

**核心测试代码示例：**

```swift
import XCTest
@testable import moneyfull_ios

final class StoreManagerTests: XCTestCase {
    
    var sut: StoreManager!
    var mockDefaults: UserDefaults!
    
    override func setUp() {
        super.setUp()
        mockDefaults = UserDefaults(suiteName: "test_store_manager")!
        mockDefaults.removePersistentDomain(forName: "test_store_manager")
        sut = StoreManager(defaults: mockDefaults)
    }
    
    override func tearDown() {
        sut = nil
        mockDefaults = nil
        super.tearDown()
    }
    
    // MARK: - SM-001: 初始化状态
    func testInitialState_IsNotPremium() {
        XCTAssertFalse(sut.isPremium)
        XCTAssertEqual(sut.fuelCredits, 0)
    }
    
    // MARK: - SM-010: 燃料包购买
    func testPurchaseFuelPack_200Credits() async throws {
        try await sut.purchaseFuelPack(productId: "com.moneyfull.fuelpack.200")
        XCTAssertEqual(sut.fuelCredits, 200)
        XCTAssertEqual(mockDefaults.integer(forKey: "fuelCredits"), 200)
    }
    
    // MARK: - SM-011: 燃料包消耗
    func testConsumeFuelCredit_Decrements() {
        sut.fuelCredits = 50
        sut.consumeFuelCredit()
        XCTAssertEqual(sut.fuelCredits, 49)
    }
    
    func testConsumeFuelCredit_AtZero_StopsAtZero() {
        sut.fuelCredits = 0
        sut.consumeFuelCredit()
        XCTAssertEqual(sut.fuelCredits, 0)
    }
    
    // MARK: - SM-012: 持久化验证
    func testFuelCredits_PersistsAcrossInit() {
        sut.fuelCredits = 100
        
        // 重新初始化模拟重启
        let newSut = StoreManager(defaults: mockDefaults)
        XCTAssertEqual(newSut.fuelCredits, 100)
    }
    
    // MARK: - SM-009: 订阅过期
    func testSubscriptionExpired_SetsPremiumFalse() {
        sut.isPremium = true
        mockDefaults.set(Date().addingTimeInterval(-86400).timeIntervalSince1970, 
                         forKey: "subscriptionExpiry")
        
        sut.verifyCurrentEntitlements()
        
        XCTAssertFalse(sut.isPremium)
    }
}
```

---

### 3.2 AI 限流逻辑测试（Phase 2）

**文件：`moneyfull_iosTests/AIChatLimitTests.swift`**

| 测试 ID | 测试场景 | 输入/前置条件 | 预期结果 | 优先级 |
|---------|---------|--------------|---------|--------|
| AL-001 | 免费用户每日限额 | `isPremium=false`, `dailyUsageCount=0` | `dailyLimit=10` | P0 |
| AL-002 | Premium 用户每日限额 | `isPremium=true` | `dailyLimit=100` | P0 |
| AL-003 | 未达限额-可继续 | `dailyUsageCount=5`, 免费用户 | `isLimitReached=false` | P0 |
| AL-004 | 达到限额-免费用户 | `dailyUsageCount=10`, 免费用户 | `isLimitReached=true` | P0 |
| AL-005 | 达到限额-有燃料包 | `dailyUsageCount=10`, `fuelCredits=50` | `isLimitReached=false` | P0 |
| AL-006 | 燃料包耗尽 | `dailyUsageCount=10`, `fuelCredits=0` | `isLimitReached=true` | P0 |
| AL-007 | 次日重置 | 跨日后首次打开 | `dailyUsageCount=0` | P0 |
| AL-008 | 剩余次数计算 | `dailyUsageCount=7`, 免费用户 | `remainingCount=3` | P1 |
| AL-009 | 达限额后消耗燃料包 | 触发限流后调用 | `fuelCredits -= 1`, 可继续使用 | P0 |
| AL-010 | Premium 软上限 | `dailyUsageCount=100`, Premium | 降级模型，不中断 | P1 |

**核心测试代码示例：**

```swift
import XCTest
@testable import moneyfull_ios

final class AIChatLimitTests: XCTestCase {
    
    var mockStoreManager: MockStoreManager!
    var mockDefaults: UserDefaults!
    
    override func setUp() {
        super.setUp()
        mockStoreManager = MockStoreManager()
        mockDefaults = UserDefaults(suiteName: "test_ai_limit")!
        mockDefaults.removePersistentDomain(forName: "test_ai_limit")
    }
    
    // MARK: - AL-001: 免费用户限额
    func testFreeUser_DailyLimit_Is10() {
        mockStoreManager.isPremium = false
        let limit = calculateDailyLimit(storeManager: mockStoreManager)
        XCTAssertEqual(limit, 10)
    }
    
    // MARK: - AL-002: Premium 用户限额
    func testPremiumUser_DailyLimit_Is100() {
        mockStoreManager.isPremium = true
        let limit = calculateDailyLimit(storeManager: mockStoreManager)
        XCTAssertEqual(limit, 100)
    }
    
    // MARK: - AL-005: 有燃料包时未达限
    func testLimitReached_WithFuelCredits_ReturnsFalse() {
        mockStoreManager.isPremium = false
        mockStoreManager.fuelCredits = 50
        
        let reached = isLimitReached(
            dailyUsageCount: 10,
            dailyLimit: 10,
            fuelCredits: mockStoreManager.fuelCredits
        )
        
        XCTAssertFalse(reached)
    }
    
    // MARK: - AL-009: 达限额后消耗燃料包
    func testConsumeFuelWhenLimitReached() {
        mockStoreManager.fuelCredits = 30
        
        // 模拟达限额
        let canContinue = mockStoreManager.fuelCredits > 0
        XCTAssertTrue(canContinue)
        
        // 消耗一次
        mockStoreManager.consumeFuelCredit()
        XCTAssertEqual(mockStoreManager.fuelCredits, 29)
    }
    
    // MARK: - AL-007: 跨日重置
    func testDailyReset_AfterMidnight() {
        let yesterday = Calendar.current.date(byAdding: .day, value: -1, to: Date())!
        let yesterdayKey = "ai_chat_usage_" + formatDate(yesterday)
        mockDefaults.set(15, forKey: yesterdayKey)
        
        let todayKey = "ai_chat_usage_" + formatDate(Date())
        let todayUsage = mockDefaults.integer(forKey: todayKey)
        
        XCTAssertEqual(todayUsage, 0) // 今日应为 0
    }
    
    // Helper
    private func calculateDailyLimit(storeManager: MockStoreManager) -> Int {
        return storeManager.isPremium ? 100 : 10
    }
    
    private func isLimitReached(dailyUsageCount: Int, dailyLimit: Int, fuelCredits: Int) -> Bool {
        let base = dailyUsageCount >= dailyLimit
        if base && fuelCredits > 0 { return false }
        return base
    }
    
    private func formatDate(_ date: Date) -> String {
        let fmt = DateFormatter()
        fmt.dateFormat = "yyyy-MM-dd"
        return fmt.string(from: date)
    }
}
```

---

### 3.3 TokenMonitor 测试（Phase 2）

**文件：`moneyfull_iosTests/TokenMonitorTests.swift`**

| 测试 ID | 测试场景 | 输入/前置条件 | 预期结果 | 优先级 |
|---------|---------|--------------|---------|--------|
| TM-001 | 记录单次 Token | `record(tokens: 150)` | 累计值正确 | P1 |
| TM-002 | 累计多次 Token | 连续调用 3 次 | 总和正确 | P1 |
| TM-003 | 按日分桶存储 | 跨日后记录 | 两日数据独立 | P1 |
| TM-004 | 查询当日消耗 | 已记录多次 | 返回正确总和 | P2 |
| TM-005 | 控制台输出 | DEBUG 模式 | 打印 Token 消耗 | P2 |

---

### 3.4 项目门控测试（Phase 3）

**文件：`moneyfull_iosTests/ProjectGateTests.swift`**

| 测试 ID | 测试场景 | 输入/前置条件 | 预期结果 | 优先级 |
|---------|---------|--------------|---------|--------|
| PG-001 | 免费用户-3个项目内 | `isPremium=false`, 2 个项目 | `canCreateProject=true` | P0 |
| PG-002 | 免费用户-达到3个上限 | `isPremium=false`, 3 个项目 | `canCreateProject=false` | P0 |
| PG-003 | Premium 用户-无限制 | `isPremium=true`, 10 个项目 | `canCreateProject=true` | P0 |
| PG-004 | 日常项目不计入 | 默认"日常收支"项目 | `customProjectCount` 不含日常 | P0 |
| PG-005 | 归档项目计入 | 有 2 个归档项目 | `customProjectCount` 包含归档 | P1 |
| PG-006 | Grandfathering 检测 | 升级前已有 5 个项目 | 标记 `hasGrandfatheredProjects` | P0 |
| PG-007 | Grandfathering 仅执行一次 | 重复启动 App | 不重复执行检测 | P1 |
| PG-008 | 老用户保留权益 | `hasGrandfatheredProjects=true` | 可继续创建 | P0 |
| PG-009 | Premium 过期后新建 | 降级后第 4 个项目 | `canCreateProject=false` | P0 |
| PG-010 | Premium 过期后保留 | 降级后已有项目 | 项目可读可写 | P0 |

**核心测试代码示例：**

```swift
import XCTest
@testable import moneyfull_ios

final class ProjectGateTests: XCTestCase {
    
    var mockStoreManager: MockStoreManager!
    var mockAppStore: MockAppStore!
    var mockDefaults: UserDefaults!
    
    override func setUp() {
        super.setUp()
        mockStoreManager = MockStoreManager()
        mockAppStore = MockAppStore()
        mockDefaults = UserDefaults(suiteName: "test_project_gate")!
        mockDefaults.removePersistentDomain(forName: "test_project_gate")
    }
    
    // MARK: - PG-001: 免费用户3个项目内
    func testFreeUser_Under3Projects_CanCreate() {
        mockStoreManager.isPremium = false
        mockAppStore.activeProjects = [
            createMockProject(name: "项目1"),
            createMockProject(name: "项目2")
        ]
        
        XCTAssertTrue(canCreateProject())
    }
    
    // MARK: - PG-002: 免费用户达到上限
    func testFreeUser_At3Projects_CannotCreate() {
        mockStoreManager.isPremium = false
        mockAppStore.activeProjects = [
            createMockProject(name: "项目1"),
            createMockProject(name: "项目2"),
            createMockProject(name: "项目3")
        ]
        
        XCTAssertFalse(canCreateProject())
    }
    
    // MARK: - PG-003: Premium 无限制
    func testPremiumUser_AnyProjectCount_CanCreate() {
        mockStoreManager.isPremium = true
        mockAppStore.activeProjects = (0..<10).map { createMockProject(name: "项目\($0)") }
        
        XCTAssertTrue(canCreateProject())
    }
    
    // MARK: - PG-004: 日常项目不计入
    func testDailyProject_NotCounted() {
        mockStoreManager.isPremium = false
        mockAppStore.activeProjects = [
            createMockProject(name: "日常收支", isActiveProject: true),
            createMockProject(name: "项目1"),
            createMockProject(name: "项目2"),
            createMockProject(name: "项目3")
        ]
        
        XCTAssertEqual(customProjectCount(), 3)
        XCTAssertFalse(canCreateProject())
    }
    
    // MARK: - PG-006: Grandfathering 检测
    func testGrandfathering_DetectsExistingProjects() {
        let projects = (0..<5).map { createMockProject(name: "旧项目\($0)") }
        
        // 模拟首次检测
        if !mockDefaults.bool(forKey: "grandfatheringChecked") {
            let count = projects.filter { !$0.isActiveProject }.count
            if count > 3 {
                mockDefaults.set(true, forKey: "hasGrandfatheredProjects")
            }
            mockDefaults.set(true, forKey: "grandfatheringChecked")
        }
        
        XCTAssertTrue(mockDefaults.bool(forKey: "hasGrandfatheredProjects"))
        XCTAssertTrue(mockDefaults.bool(forKey: "grandfatheringChecked"))
    }
    
    // MARK: - PG-008: 老用户保留权益
    func testGrandfatheredUser_CanCreate() {
        mockStoreManager.isPremium = false
        mockDefaults.set(true, forKey: "hasGrandfatheredProjects")
        mockAppStore.activeProjects = (0..<5).map { createMockProject(name: "项目\($0)") }
        
        // 老用户可继续创建
        XCTAssertTrue(canCreateProject())
    }
    
    // MARK: - Helpers
    private func canCreateProject() -> Bool {
        if mockStoreManager.isPremium { return true }
        if mockDefaults.bool(forKey: "hasGrandfatheredProjects") { return true }
        return customProjectCount() < 3
    }
    
    private func customProjectCount() -> Int {
        return mockAppStore.activeProjects.filter { !$0.isActiveProject }.count
    }
    
    private func createMockProject(name: String, isActiveProject: Bool = false) -> Project {
        // 返回模拟 Project 对象
        ...
    }
}

// Mock AppStore
class MockAppStore: ObservableObject {
    @Published var activeProjects: [Project] = []
    @Published var archivedProjects: [Project] = []
}
```

---

### 3.5 LLM Service 测试（Phase 2）

**文件：`moneyfull_iosTests/LLMServiceTests.swift`**

| 测试 ID | 测试场景 | 输入/前置条件 | 预期结果 | 优先级 |
|---------|---------|--------------|---------|--------|
| LM-001 | 正常解析记账 | "买咖啡25元" | 返回 success + 正确金额分类 | P0 |
| LM-002 | 模糊表达处理 | "花了50" | 返回 need_clarification | P1 |
| LM-003 | 活跃项目优先 | 有活跃项目 + 记忆规则 | 归入活跃项目 | P0 |
| LM-004 | 无活跃项目兜底 | 无活跃项目 | project_name = null | P1 |
| LM-005 | Token 记录 | API 返回 usage | 调用 TokenMonitor.record | P1 |
| LM-006 | 网络错误处理 | 网络超时 | 抛出错误，不崩溃 | P1 |

---

## 四、集成测试用例设计

### 4.1 StoreKit 集成测试（需 TestFlight / Sandbox）

**文件：`moneyfull_iosTests/StoreKitIntegrationTests.swift`**

| 测试 ID | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|--------|
| SK-001 | 完整购买流程 | 加载产品 → 购买 → 验证 | `isPremium = true` | P0 |
| SK-002 | 订阅续费后状态 | 购买 → 模拟续费 → 验证 | 状态持续有效 | P0 |
| SK-003 | 订阅取消后状态 | 购买 → 取消 → 到期 → 验证 | `isPremium = false` | P0 |
| SK-004 | 恢复购买 | 新设备 → 恢复购买 | 历史订阅恢复 | P0 |
| SK-005 | 燃料包消耗联动 | 购买燃料包 → AI 调用 → 验证 | `fuelCredits` 递减 | P0 |
| SK-006 | 订阅 + 燃料包叠加 | 有订阅 + 有燃料包 | 优先消耗订阅额度 | P1 |
| SK-007 | 购买中断恢复 | 购买中 App 被杀 | 下次启动恢复验证 | P1 |

**核心测试代码示例：**

```swift
import XCTest
import StoreKit
@testable import moneyfull_ios

@available(iOS 15.0, *)
final class StoreKitIntegrationTests: XCTestCase {
    
    var sut: StoreManager!
    
    override func setUp() async throws {
        try await super.setUp()
        // 使用 StoreKit 测试环境
        sut = StoreManager()
    }
    
    // MARK: - SK-001: 完整购买流程
    func testFullPurchaseFlow_MonthlySubscription() async throws {
        // 1. 加载产品
        let products = try await Product.products(for: ["com.moneyfull.premium.monthly"])
        guard let monthlyProduct = products.first else {
            XCTFail("无法加载月卡产品")
            return
        }
        
        // 2. 购买
        let result = try await monthlyProduct.purchase()
        
        // 3. 验证
        switch result {
        case .success(let verification):
            let transaction = try checkVerified(verification)
            XCTAssertTrue(sut.isPremium)
            XCTAssertNotNil(transaction)
        case .userCancelled:
            XCTFail("测试不应取消")
        case .pending:
            XCTFail("测试不应挂起")
        @unknown default:
            XCTFail("未知状态")
        }
    }
    
    // MARK: - SK-005: 燃料包消耗联动
    func testFuelPackConsumption_Integration() async throws {
        // 1. 购买燃料包
        let products = try await Product.products(for: ["com.moneyfull.fuelpack.200"])
        guard let fuelPack = products.first else { return }
        
        let result = try await fuelPack.purchase()
        if case .success(let verification) = result {
            _ = try checkVerified(verification)
        }
        
        // 2. 验证燃料包到账
        XCTAssertEqual(sut.fuelCredits, 200)
        
        // 3. 模拟 AI 调用消耗
        sut.consumeFuelCredit()
        XCTAssertEqual(sut.fuelCredits, 199)
    }
    
    private func checkVerified<T>(_ result: VerificationResult<T>) throws -> T {
        switch result {
        case .unverified:
            throw StoreError.failedVerification
        case .verified(let value):
            return value
        }
    }
}

enum StoreError: Error {
    case failedVerification
}
```

---

### 4.2 跨模块集成测试

**文件：`moneyfull_iosTests/CrossModuleIntegrationTests.swift`**

| 测试 ID | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|--------|
| CM-001 | 订阅 → AI 限流 | 购买 Premium → AI 调用 50 次 | 均成功，不触发限流 | P0 |
| CM-002 | 燃料包 → AI 限流 | 免费用户 + 燃料包 → 超 10 次 | 燃料包消耗，继续可用 | P0 |
| CM-003 | 订阅过期 → 项目门控 | Premium 过期 → 新建第 4 个项目 | 被拦截，弹出 Paywall | P0 |
| CM-004 | Grandfathering → 订阅 | 老用户 → 购买 Premium | 解锁无限项目 | P1 |
| CM-005 | 端到端购买流 | Paywall → 购买 → 验证 → 返回 | 全流程顺畅 | P0 |

---

## 五、UI 测试用例设计

### 5.1 Paywall UI 测试

**文件：`moneyfull_iosUITests/PaywallUITests.swift`**

| 测试 ID | 测试场景 | 测试步骤 | 验证点 | 优先级 |
|---------|---------|---------|--------|--------|
| PU-001 | Paywall 展示 | 触发 Paywall | 3 个定价卡片可见 | P0 |
| PU-002 | 年卡高亮 | 打开 Paywall | 年卡有"主推"标识 | P0 |
| PU-003 | 购买按钮可点击 | 点击月卡购买按钮 | 触发购买流程 | P0 |
| PU-004 | 恢复购买入口 | 点击"恢复购买" | 触发恢复流程 | P1 |
| PU-005 | 兑换码入口 | 点击"兑换码" | 弹出兑换界面 | P1 |
| PU-006 | 滑动关闭 | 向下滑动 Paywall | 正常关闭 | P1 |

**核心测试代码示例：**

```swift
import XCTest

final class PaywallUITests: XCTestCase {
    
    let app = XCUIApplication()
    
    override func setUpWithError() throws {
        continueAfterFailure = false
        app.launch()
    }
    
    // MARK: - PU-001: Paywall 展示
    func testPaywall_Shows3PricingCards() {
        // 触发 Paywall（通过 AI 限额耗尽或新建项目）
        triggerPaywall()
        
        // 验证 3 个定价卡片
        XCTAssertTrue(app.staticTexts["月卡"].waitForExistence(timeout: 5))
        XCTAssertTrue(app.staticTexts["年卡"].exists)
        XCTAssertTrue(app.staticTexts["终身"].exists)
    }
    
    // MARK: - PU-002: 年卡高亮
    func testPaywall_AnnualPlanHighlighted() {
        triggerPaywall()
        
        // 验证年卡有"主推"标识
        let annualBadge = app.staticTexts["主推"]
        XCTAssertTrue(annualBadge.exists)
    }
    
    // MARK: - PU-003: 购买按钮
    func testPaywall_PurchaseButton_Tappable() {
        triggerPaywall()
        
        let purchaseButton = app.buttons["订阅月卡"]
        XCTAssertTrue(purchaseButton.exists)
        XCTAssertTrue(purchaseButton.isEnabled)
        purchaseButton.tap()
        
        // 验证触发购买（StoreKit 弹窗）
        XCTAssertTrue(app.alerts.firstMatch.waitForExistence(timeout: 3))
    }
    
    // MARK: - PU-005: 兑换码入口
    func testPaywall_OfferCodeEntry() {
        triggerPaywall()
        
        let offerCodeButton = app.buttons["兑换码"]
        XCTAssertTrue(offerCodeButton.exists)
        offerCodeButton.tap()
        
        // 验证弹出兑换界面
        XCTAssertTrue(app.otherElements["OfferCodeSheet"].waitForExistence(timeout: 3))
    }
    
    // MARK: - Helpers
    private func triggerPaywall() {
        // 通过导航触发 Paywall
        // 方式1: AI 限额耗尽
        // 方式2: 新建第 4 个项目
        // 方式3: Profile 页面入口
        app.tabBars.buttons["个人中心"].tap()
        app.buttons["升级 Premium"].tap()
    }
}
```

---

### 5.2 项目创建门控 UI 测试

**文件：`moneyfull_iosUITests/ProjectGateUITests.swift`**

| 测试 ID | 测试场景 | 测试步骤 | 验证点 | 优先级 |
|---------|---------|---------|--------|--------|
| PG-UI-001 | 免费用户创建成功 | 创建第 1-3 个项目 | 均创建成功 | P0 |
| PG-UI-002 | 免费用户被拦截 | 创建第 4 个项目 | 弹出升级提示 | P0 |
| PG-UI-003 | Premium 无限制 | Premium 用户创建第 4 个 | 创建成功 | P0 |
| PG-UI-004 | 升级后继续创建 | 被拦截 → 购买 Premium → 继续 | 创建成功 | P0 |
| PG-UI-005 | 老用户保留 | 已有 5 个项目的老用户 | 可继续编辑 | P0 |

---

### 5.3 AI 限额 UI 测试

**文件：`moneyfull_iosUITests/AIChatLimitUITests.swift`**

| 测试 ID | 测试场景 | 测试步骤 | 验证点 | 优先级 |
|---------|---------|---------|--------|--------|
| AI-UI-001 | 限额提示展示 | 免费用户发 10 条消息 | 显示"今日额度已用完" | P0 |
| AI-UI-002 | 燃料包入口 | 点击"购买燃料包" | 弹出燃料包 Sheet | P0 |
| AI-UI-003 | 剩余次数显示 | 发送 7 条消息 | 显示"剩余 3 次" | P1 |
| AI-UI-004 | 次日重置 UI | 跨日后打开 | 显示"剩余 10 次" | P1 |

---

## 六、测试数据管理

### 6.1 测试数据工厂

```swift
// TestHelpers/TestDataFactory.swift
enum TestDataFactory {
    
    static func createMockProject(
        name: String = "测试项目",
        isActiveProject: Bool = false,
        budget: Double = 1000
    ) -> Project {
        let project = Project()
        project.name = name
        project.isActiveProject = isActiveProject
        project.budget = budget
        return project
    }
    
    static func createMockTransaction(
        amount: Double = 100,
        type: TransactionType = .expense,
        category: String = "餐饮",
        project: Project? = nil
    ) -> Transaction {
        let transaction = Transaction()
        transaction.amount = amount
        transaction.type = type
        transaction.categoryName = category
        transaction.project = project
        return transaction
    }
    
    static func createPremiumUser() -> MockStoreManager {
        let manager = MockStoreManager()
        manager.isPremium = true
        return manager
    }
    
    static func createFreeUserWithFuelPack(credits: Int = 200) -> MockStoreManager {
        let manager = MockStoreManager()
        manager.isPremium = false
        manager.fuelCredits = credits
        return manager
    }
}
```

### 6.2 测试套件组织

```
moneyfull_iosTests/
├── Unit/
│   ├── StoreManagerTests.swift
│   ├── AIChatLimitTests.swift
│   ├── TokenMonitorTests.swift
│   ├── ProjectGateTests.swift
│   ├── LLMServiceTests.swift
│   └── GrandfatheringTests.swift
├── Integration/
│   ├── StoreKitIntegrationTests.swift
│   └── CrossModuleIntegrationTests.swift
├── Helpers/
│   ├── MockStoreManager.swift
│   ├── MockLLMService.swift
│   ├── MockAppStore.swift
│   └── TestDataFactory.swift
└── moneyfull_iosTests.swift

moneyfull_iosUITests/
├── PaywallUITests.swift
├── ProjectGateUITests.swift
├── AIChatLimitUITests.swift
└── moneyfull_iosUITests.swift

StoreKit/
└── StoreKitConfiguration.storekit
```

---

## 七、CI/CD 集成

### 7.1 GitHub Actions 配置

```yaml
# .github/workflows/test.yml
name: 商业化模块测试

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  unit-tests:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: 选择 Xcode 版本
        run: sudo xcode-select -s /Applications/Xcode_15.0.app
      
      - name: 运行单元测试
        run: |
          xcodebuild test \
            -project moneyfull_ios.xcodeproj \
            -scheme moneyfull_ios \
            -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.0' \
            -only-testing:moneyfull_iosTests \
            -enableCodeCoverage YES \
            | xcpretty
      
      - name: 上传测试报告
        uses: actions/upload-artifact@v3
        with:
          name: test-results
          path: fastlane/test_output
      
      - name: 代码覆盖率报告
        run: |
          xcrun xccov view --report --json \
            fastlane/test_output/moneyfull_ios.xcresult \
            > coverage.json
```

### 7.2 测试触发规则

| 触发场景 | 执行测试范围 |
|---------|-------------|
| PR 提交 | 单元测试 + 集成测试 |
| 合并到 develop | 全量测试 + UI 测试 |
| 合并到 main | 全量测试 + 性能测试 + TestFlight 构建 |
| 定时（每日） | 回归测试套件 |

---

## 八、测试覆盖率目标

| 模块 | 目标覆盖率 | 关键路径 |
|------|-----------|---------|
| StoreManager | ≥ 90% | 购买、恢复、状态验证 |
| AI 限流逻辑 | ≥ 85% | 限额判断、燃料包消耗、重置 |
| 项目门控 | ≥ 85% | 创建判断、Grandfathering |
| TokenMonitor | ≥ 80% | 记录、查询、持久化 |
| LLMService | ≥ 75% | 解析、错误处理、Token 记录 |
| **整体商业化模块** | **≥ 80%** | — |

---

## 九、测试执行计划

### 9.1 开发阶段对应

| Phase | 测试任务 | 依赖 | 产出 |
|-------|---------|------|------|
| Phase 1 | StoreManager 单元测试 + StoreKit 集成测试 | StoreManager.swift 完成 | 测试报告 |
| Phase 2 | AI 限流测试 + TokenMonitor 测试 | AIChatView 修改完成 | 测试报告 |
| Phase 3 | 项目门控测试 + Grandfathering 测试 | AppStore + NewProjectView 修改完成 | 测试报告 |
| Phase 4 | Paywall UI 测试 + 端到端测试 | PaywallView 完成 | 测试报告 |
| Phase 5 | ProfileView 集成测试 | ProfileView 修改完成 | 测试报告 |

### 9.2 测试 Checklist

**Phase 1 测试清单：**
- [ ] StoreManager 初始化状态正确
- [ ] 产品加载成功/失败处理
- [ ] 月卡/年卡/终身购买流程
- [ ] 燃料包购买和持久化
- [ ] 恢复购买功能
- [ ] 订阅过期状态更新
- [ ] 并发购买防护

**Phase 2 测试清单：**
- [ ] 免费用户 10 次/天限额
- [ ] Premium 用户 100 次/天限额
- [ ] 燃料包叠加逻辑
- [ ] 次日重置功能
- [ ] Token 记录准确性
- [ ] 限额后 UI 提示

**Phase 3 测试清单：**
- [ ] 免费用户 3 个项目上限
- [ ] 日常项目不计入限制
- [ ] Grandfathering 一次性检测
- [ ] 老用户保留权益
- [ ] Premium 过期后项目保留
- [ ] 降级后新建拦截

**Phase 4 测试清单：**
- [ ] Paywall 三档定价展示
- [ ] 年卡主推高亮
- [ ] 购买流程完整性
- [ ] 恢复购买入口
- [ ] 兑换码入口
- [ ] AI 限额触发场景
- [ ] 项目上限触发场景
- [ ] 预算超支触发场景

**Phase 5 测试清单：**
- [ ] 订阅状态卡片展示
- [ ] 升级/管理入口
- [ ] 兑换码入口
- [ ] 燃料包剩余显示

---

## 十、常见测试问题与解决方案

### 10.1 StoreKit 测试环境问题

**问题：** 测试时无法加载产品列表
**解决：**
1. 确认 StoreKit Configuration 文件已正确配置
2. 检查 Scheme 中已选择 StoreKit Configuration
3. 确认 Product ID 与配置文件一致

### 10.2 异步测试超时

**问题：** StoreKit 购买测试超时
**解决：**
```swift
func testPurchase() async throws {
    let expectation = expectation(description: "购买完成")
    
    Task {
        let result = try await product.purchase()
        expectation.fulfill()
    }
    
    await fulfillment(of: [expectation], timeout: 10)
}
```

### 10.3 测试数据隔离

**问题：** 测试间数据互相影响
**解决：**
- 使用独立的 UserDefaults suite
- 每个测试 setUp/tearDown 清理数据
- 使用 Mock 隔离外部依赖

---

## 附录 A：测试用例速查表

| 模块 | 测试文件 | 用例数 | 优先级分布 |
|------|---------|--------|-----------|
| StoreManager | StoreManagerTests.swift | 14 | P0:7, P1:5, P2:2 |
| AI 限流 | AIChatLimitTests.swift | 10 | P0:6, P1:3, P2:1 |
| TokenMonitor | TokenMonitorTests.swift | 5 | P1:3, P2:2 |
| 项目门控 | ProjectGateTests.swift | 10 | P0:6, P1:3, P2:1 |
| LLMService | LLMServiceTests.swift | 6 | P0:2, P1:3, P2:1 |
| StoreKit 集成 | StoreKitIntegrationTests.swift | 7 | P0:5, P1:2 |
| 跨模块集成 | CrossModuleIntegrationTests.swift | 5 | P0:3, P1:2 |
| Paywall UI | PaywallUITests.swift | 6 | P0:3, P1:3 |
| 项目门控 UI | ProjectGateUITests.swift | 5 | P0:5 |
| AI 限额 UI | AIChatLimitUITests.swift | 4 | P0:2, P1:2 |
| **总计** | — | **72** | **P0:42, P1:26, P2:4** |

---

## 附录 B：测试命令速查

```bash
# 运行全部单元测试
xcodebuild test -project moneyfull_ios.xcodeproj -scheme moneyfull_ios -destination 'platform=iOS Simulator,name=iPhone 15'

# 运行指定测试类
xcodebuild test -project moneyfull_ios.xcodeproj -scheme moneyfull_ios -only-testing:moneyfull_iosTests/StoreManagerTests

# 运行指定测试方法
xcodebuild test -project moneyfull_ios.xcodeproj -scheme moneyfull_ios -only-testing:moneyfull_iosTests/StoreManagerTests/testInitialState_IsNotPremium

# 生成代码覆盖率报告
xcodebuild test -project moneyfull_ios.xcodeproj -scheme moneyfull_ios -enableCodeCoverage YES

# 运行 UI 测试
xcodebuild test -project moneyfull_ios.xcodeproj -scheme moneyfull_ios -only-testing:moneyfull_iosUITests
```

---

*文档版本：V1.0 | 最后更新：2026-06-25*
