好，现在我清楚了整个系统的逻辑，来说清楚老用户面对的真实处境和我的方案建议。

---

## 老用户当前实际状态分析

通过读代码，老用户分 3 种情况：

| 用户类型 | `hasSeenOnboarding` | `hasCompletedAssessment` | `userPersonaType` | `aiPersonaSystemPrompt` |
|---------|--------------------|--------------------------|--------------------|--------------------------|
| 老用户（测评前安装） | `true` | `false` | 无 | 无 |
| 老用户（已完成过测评） | `true` | `true` | 有 | 有（或无，视版本） |
| 新用户（本次流程完成） | `true` | `true` | 有 | 有 |

**当前代码里老用户（无测评）的实际体验：**
- 跳过欢迎页 ✅
- 直接进首页 ✅
- 打开 AI 对话 → `assessed = false`，所以**不播放任何 onboarding 预制消息**，也没有 persona 系统 prompt 注入，AI 以通用模式回复

AI 并不会崩溃，但它对用户一无所知——等于一个没有记忆的助手。

---

## 核心产品问题

> 强制老用户做测评 = 打断他们的正常使用节奏，可能引起反感
> 完全不引导 = AI 画像价值对他们是零，浪费了你做这套系统的价值

所以这是一个「用户体验连续性」vs「功能价值传递」的平衡问题。

---

## 我的建议方案：三层递进，不强制

### 第一层：无感兜底（最高优先级，必做）

在 `ContextManager.swift` 或 `AIChatView` 的 `generatePersonaSystemPrompt` 逻辑里，当 `userPersonaType` 为空时，**自动赋一个最通用的默认 persona**（建议用 `.steady` 稳健积累者，它是最中性的那个）并生成基础系统 prompt。

效果：老用户的 AI 有基本的角色感，不会像白板，也不强迫他们做任何事。

### 第二层：软引导（主动但不打断）

在 `ProfileView` 的功能设置网格里加一个入口：

```
「更新记账画像」🧩
```

点进去直接是 `AssessmentView`（不含欢迎页），完成后重新跑 `PersonaConfigExecutor`，更新 `aiPersonaSystemPrompt`。这是「用户有空、有意愿时自己来做」的路径。

### 第三层：时机触发（可选，效果最好）

当老用户**第一次打开 AI 对话页**且没有 persona 数据时，显示一条非阻断的提示消息（非弹窗）：

```
小满：我们还不太熟，做个 30 秒小测评，我能更懂你～
      [开始测评]   [算了，直接聊]
```

触发条件：`hasCompletedAssessment == false` 且 `messages.isEmpty`。这条消息作为一条 AI 气泡出现，和正常对话融为一体，不会阻断任何操作。

---

## 完整判断逻辑

```
App 启动
  ↓
hasSeenOnboarding?
  ├─ false → 走欢迎页 + 测评（新用户流程）
  └─ true  → 直接进首页

打开 AI 对话
  ↓
hasCompletedAssessment?
  ├─ true  && hasCompletedOnboardingChat = false → 播放 onboarding 预制消息（已实现）
  ├─ true  && hasCompletedOnboardingChat = true  → 正常对话
  └─ false → 显示「做个测评让我更懂你」引导气泡（新增）
             [用户选择开始] → AssessmentView（sheet弹出，不替换页面）
                          → 完成后 PersonaConfigExecutor + playOnboardingMessages
             [用户选择跳过] → hasCompletedOnboardingChat = true，不再提示
```

---

## 关于是否需要 Re-assessment

老用户的行为和习惯可能和当初回答的不一样（比如从上班族变成了自由职业者），这是一个**长期产品价值点**，但当前阶段不建议做「定期重测提醒」，理由：

- 数据基础还不够（用户量和使用时长）
- 频繁提醒测评会降低产品可信度
- 更好的时机是：用户主动改了收入类型或创建了新的搞钱项目时，系统问「要更新你的画像吗」——这是上下文触发，不是定时提醒

**结论：当前只需要做第一层（silent fallback）+ 第二层（Profile 入口），第三层 AI 气泡引导看你的排期。**