# 钱小满 · 经营看板 PRD

> 版本：V1.1（2026-07-10）
> 产品定位：解决斜杠青年/自由职业者"钱在哪、赚没赚、剩多少"的生存级需求

***

## 一、产品背景

### 1.1 用户痛点

斜杠青年/自由职业者的收入呈脉冲式、不稳定，核心焦虑是：

- 手里有多少活钱？能不能撑过空窗期？
- 表面流水高，实际没收到钱？
- 卖得多，但不知道是赚还是亏？
- 哪条业务线才是真正赚钱的？

### 1.2 与普通记账的区别

普通记账只记录流水，经营看板的核心差异是**支持按业务线拆分**，并提供现金流、收入确认、成本结构、利润分析四维度视角。

### 1.3 设计原则

- **收付实现制**：钱没进口袋，就不算收入
- **三层利润**：毛利 → 经营净利 → 可支配收入，层层递进
- **低门槛**：基础数据自动计算，高级功能可选配置
- **单项目聚焦**：本期不做跨项目全局聚合，聚焦单个「搞钱模式」项目
- **零新增字段**：记账主流程不因看板需求增加任何新字段，所有维度数据来自现有字段的重新聚合计算
- **业务线 = 收入分类**：「业务线」不是一个独立的新标签，就是项目内的收入分类。选「小红书佣金」这个分类，就隐含了这是「自媒体」业务线的收入，看板侧对分类聚合即得业务线占比。

***

## 二、四维度看板设计

### 2.1 现金流维度

**核心问题**：这个项目的钱，活不活？够不够撑？

| 指标         | 定义                 | 数据来源 | 计算逻辑                              |
| ---------- | ------------------ | ---- | --------------------------------- |
| 累计可用资金     | 这个项目从开始到现在，手里剩多少钱  | 系统计算 | `累计收入 - 累计支出`                     |
| 当月净现金流     | 这个月这个项目是"造血"还是"烧钱" | 系统计算 | `当月收入 - 当月支出`（按月筛选 transactions）  |
| 经营/个人现金流拆分 | 哪些是经营花的，哪些是个人花的    | 用户标记 | 记账时选择「经营支出」或「个人支出」，系统按标记归类        |
| 未来30天刚性支出  | 不管做不做项目都要花的固定开支    | 用户预设 | 用户录入固定成本项（社保、房租、软件等），系统汇总30天内到期金额 |

**看板展示**：

```
┌──────────────────────────────────────────────┐
│  💰 现金流                                   │
├──────────────────────────────────────────────┤
│  累计可用资金            ¥28,500             │
│                                              │
│  本月净现金流            +¥12,300            │
│  ████████████░░░░░░░░  经营 +¥15,000        │
│  ████░░░░░░░░░░░░░░░░  个人 -¥2,700        │
│                                              │
│  ⚠️ 未来30天刚性支出    ¥8,200              │
│  （社保¥2,100 + 房租¥3,500 + 软件¥800 + ...）│
│                                              │
│  💡 按当前消耗速度，可支撑 2.3个月           │
└──────────────────────────────────────────────┘
```

***

### 2.2 收入维度

**核心问题**：到底赚了多少真钱？哪些还在别人口袋里？

**设计原则**：采用收付实现制——钱没进口袋，就不算收入。待回款作为独立的「应收账款」管理，不计入收入。

| 指标      | 定义           | 数据来源 | 计算逻辑                 |
| ------- | ------------ | ---- | -------------------- |
| 已到账收入   | 真正收到的钱       | 用户记账 | 每一笔「收入」记录都是已到账的真金白银  |
| 待回款总额   | 别人欠你的钱，还没给   | 用户录入 | 独立的「应收账款」记录，不计入收入    |
| 逾期未回款   | 超过预计日期还没收到的钱 | 系统判断 | 应收账款超过预计回款日期，自动标记为逾期 |
| 业务线收入占比 | 哪条业务线是收入主力   | 系统计算 | 按收入分类（`category`）聚合已到账收入，计算占比。分类即业务线，无需新字段 |
| 月度收入趋势  | 收入走势如何       | 系统计算 | 按月聚合已到账收入，绘制柱状图      |

**看板展示**：

```
┌──────────────────────────────────────────────┐
│  💵 收入分析                                 │
├──────────────────────────────────────────────┤
│  本月已到账收入        ¥15,000               │
│  累计已到账收入        ¥47,000               │
├──────────────────────────────────────────────┤
│  📋 应收账款                   管理 →        │
│  待回款总额            ¥12,000               │
│  ┌─────────────────────────────────────┐     │
│  │ 品牌设计外包A     ¥8,000  7月15日   │     │
│  │ 小红书佣金        ¥4,000  7月20日   │     │
│  │ ⚠️ Logo设计B      ¥3,000  已逾期15天│     │
│  └─────────────────────────────────────┘     │
├──────────────────────────────────────────────┤
│  业务线收入占比        [环形图]              │
│  设计外包  60%  │  自媒体  25%  │  其他 15%  │
├──────────────────────────────────────────────┤
│  月度收入趋势          [柱状图]              │
└──────────────────────────────────────────────┘
```

**为什么用收付实现制？**

普通人心智模型：收到钱 = 赚到了，没收到 = 还没赚到。

如果把待回款也算作收入，看板上的数字是虚的——用户会误以为自己有钱，实际钱还在别人口袋里。收付实现制保证看板上的每一个数字都是真实的。

***

### 2.3 成本维度

**核心问题**：钱花到哪里去了？哪些是必要成本？

**三层成本拆分**：

| 成本类型   | 定义            | 示例                | 数据来源       |
| ------ | ------------- | ----------------- | ---------- |
| 直接成本   | 做这单才花的钱，跟着业务走 | 买素材、付外包费、平台佣金、快递费 | 记账时按分类自动判断 |
| 固定经营成本 | 不管做不做这单都要花的钱  | 社保、房租、软件会员、设备折旧   | 用户预设，一次性配置 |
| 个人生活成本 | 跟经营无关的个人开销    | 自己吃饭、娱乐、购物        | 记账时手动标记    |

| 指标     | 定义       | 数据来源 | 计算逻辑                                      |
| ------ | -------- | ---- | ----------------------------------------- |
| 直接成本   | 做这个项目花的钱 | 系统计算 | 预算分类设置时标记 `isDirectCost: true`，记账时按分类自动归类 |
| 固定经营成本 | 月度固定支出   | 用户预设 | 用户在「固定成本管理」中录入，系统按月汇总                     |
| 个人生活成本 | 个人开销     | 用户标记 | 记账时选择「个人支出」标记                             |
| 成本结构占比 | 三种成本各占多少 | 系统计算 | 按类型聚合，计算占比                                |

**看板展示**：

```
┌──────────────────────────────────────────────┐
│  📉 成本分析                                 │
├──────────────────────────────────────────────┤
│  本月成本构成                                  │
│  ┌──────────┬──────────┬──────────┐          │
│  │ 直接成本  │ 固定成本  │ 生活成本  │          │
│  │ ¥12,000  │ ¥6,080   │ ¥4,500   │          │
│  │ 53%      │ 27%      │ 20%      │          │
│  └──────────┴──────────┴──────────┘          │
├──────────────────────────────────────────────┤
│  直接成本明细（按分类）                        │
│  🎨 素材购买    ¥3,200                       │
│  👥 外包费用    ¥5,500                       │
│  📦 平台佣金    ¥2,100                       │
│  ✈️ 差旅       ¥1,200                        │
├──────────────────────────────────────────────┤
│  固定经营成本明细               管理 →       │
│  🏥 社保公积金    ¥2,100/月                  │
│  🏠 房租         ¥3,500/月                   │
│  💻 软件会员     ¥480/月                     │
│  合计            ¥6,080/月                   │
├──────────────────────────────────────────────┤
│  成本趋势  [折线图]                          │
└──────────────────────────────────────────────┘
```

**成本性质判断逻辑（简化版）**：

不需要用户每次记账都选成本性质，而是在预算分类设置时预设：

- 「素材购买」「外包费」「平台佣金」「快递费」→ 默认直接成本
- 「餐饮」「娱乐」「购物」→ 默认个人生活成本
- 固定成本独立管理，不通过记账录入

用户可以在记账时覆盖默认值，但大多数情况不需要操作。

***

### 2.4 利润维度

**核心问题**：到底赚了多少钱？不是流水好看就行。

**三层利润瀑布**：

```
总收入
  - 直接成本
  ─────────────────────
  = 毛利              （做这单赚了多少）
  - 固定经营成本
  ─────────────────────
  = 经营净利润        （经营效率如何）
  - 个人生活成本
  - 税费预留
  ─────────────────────
  = 个人可支配收入    （真正能花的钱）
```

| 指标      | 定义          | 数据来源   | 计算逻辑                    |
| ------- | ----------- | ------ | ----------------------- |
| 毛利      | 做这单业务本身赚了多少 | 系统计算   | `已到账收入 - 直接成本`          |
| 毛利率     | 毛利占收入的比例    | 系统计算   | `毛利 / 已到账收入 × 100%`     |
| 经营净利润   | 扣除固定成本后的利润  | 系统计算   | `毛利 - 固定经营成本`           |
| 个人可支配收入 | 真正能花的钱      | 系统计算   | `经营净利润 - 个人生活成本 - 税费预留` |
| 税费预留    | 预留的税费金额     | 用户设置比例 | 用户设置税率（如20%），系统自动计算预留金额 |

**看板展示**：

```
┌──────────────────────────────────────────────┐
│  📈 利润分析                                 │
├──────────────────────────────────────────────┤
│  本月利润瀑布                                  │
│                                              │
│  总收入          ████████████████████ ¥47,000│
│  - 直接成本      ████████░░░░░░░░░░░░ ¥12,000│
│  ──────────────────────────────────────────   │
│  = 毛利          ████████████░░░░░░░░ ¥35,000│
│                  毛利率 74%                   │
│  - 固定成本      ██████░░░░░░░░░░░░░░ ¥6,080 │
│  ──────────────────────────────────────────   │
│  = 经营净利润    ████████░░░░░░░░░░░░ ¥28,920│
│  - 生活成本      ████░░░░░░░░░░░░░░░░ ¥4,500 │
│  - 税费预留      ███░░░░░░░░░░░░░░░░░ ¥5,784 │
│  ──────────────────────────────────────────   │
│  = 可支配收入    ████░░░░░░░░░░░░░░░░ ¥18,636│
├──────────────────────────────────────────────┤
│  月度利润趋势  [折线图]                       │
│  ─ 毛利                                      │
│  ─ 经营净利润                                │
│  ─ 可支配收入                                │
└──────────────────────────────────────────────┘
```

***

## 三、数据录入交互设计

### 3.1 收入录入

收入记账保持简单，和现有流程完全一致，**无需任何额外字段**：

```
┌──────────────────────────────────────────────┐
│  记一笔收入                                  │
│                                              │
│  金额    [¥5,000]                            │
│  分类    [设计外包 ▾]                         │
│  日期    [今天 ▾]                             │
│  备注    [品牌设计第一期款]                   │
│                                              │
│  [保存]                                      │
└──────────────────────────────────────────────┘
```

> **为什么不需要「业务线」字段？**
> 用户选择的「分类」本身就是业务线标识。「设计外包」「小红书佣金」「抖音带货」都是现有分类，看板侧对 `transaction.category` 聚合就能得到各业务线收入占比，录入侧完全无感。

### 3.2 支出录入

支出记账**对用户而言流程不变**，「成本性质」是一个系统自动写入的透明字段，用户绝大多数情况下看不到它：

```
┌──────────────────────────────────────────────┐
│  记一笔支出                                  │
│                                              │
│  金额    [¥800]                              │
│  分类    [素材购买 ▾]                         │
│  日期    [今天 ▾]                             │
│  备注    [买了一套设计素材]                   │
│                                              │
│  [保存]            [更多选项 ▾]              │  ← 展开才显示成本性质覆盖
└──────────────────────────────────────────────┘
```

**展开「更多选项」后**（仅在搞钱模式项目下的支出录入中出现）：

```
  成本性质  ● 直接成本  ○ 个人支出
  💡 已根据分类「素材购买」自动判断，通常无需修改
```

> **交互原则**：「成本性质」是后台隐藏属性，不是前台显式字段。用户选择分类后系统自动写入，只有用户主动点「更多选项」才能看到并覆盖。普通情况下记一笔支出的字段数量与非搞钱模式项目**完全相同**。

**默认规则**：

| 分类                   | 默认成本性质 | 可覆盖 |
| -------------------- | ------ | --- |
| 素材购买、外包费、平台佣金、快递费、差旅 | 直接成本   | ✅   |
| 餐饮、娱乐、购物、交通（个人）      | 个人支出   | ✅   |

### 3.3 应收账款管理

独立入口，不混在记账流程中：

```
┌──────────────────────────────────────────────┐
│  📋 应收账款管理                 + 新增应收   │
├──────────────────────────────────────────────┤
│  待回款总额            ¥12,000               │
│  逾期金额              ¥3,000  ⚠️           │
├──────────────────────────────────────────────┤
│  进行中                                  筛选 │
│  ┌─────────────────────────────────────┐     │
│  │ 品牌设计外包A                       │     │
│  │ 客户：张三公司                      │     │
│  │ 金额：¥8,000                        │     │
│  │ 预计回款：7月15日                   │     │
│  │ [标记到账]  [编辑]                  │     │
│  ├─────────────────────────────────────┤     │
│  │ 小红书佣金                          │     │
│  │ 客户：MCN机构                       │     │
│  │ 金额：¥4,000                        │     │
│  │ 预计回款：7月20日                   │     │
│  │ [标记到账]  [编辑]                  │     │
│  └─────────────────────────────────────┘     │
├──────────────────────────────────────────────┤
│  逾期                                    1笔 │
│  ┌─────────────────────────────────────┐     │
│  │ ⚠️ Logo设计B                        │     │
│  │ 客户：李四工作室                    │     │
│  │ 金额：¥3,000                        │     │
│  │ 预计回款：6月25日（已逾期15天）     │     │
│  │ [标记到账]  [编辑]  [催款提醒]      │     │
│  └─────────────────────────────────────┘     │
└──────────────────────────────────────────────┘
```

**新增应收**：

```
┌──────────────────────────────────────────────┐
│  + 新增应收账款                              │
│                                              │
│  客户名称    [张三公司]                      │
│  业务描述    [品牌设计外包]                  │
│  应收金额    [¥8,000]                        │
│  预计回款日   [7月15日 ▾]                    │
│  备注        [合同分两期，这是尾款]          │
│                                              │
│  [保存]                                      │
└──────────────────────────────────────────────┘
```

**标记到账**：

当应收账款收到钱时，点击「标记到账」→ 跳转到记收入页面，自动填充金额和备注，用户确认保存即可。

### 3.4 固定成本管理

一次性配置，每月自动计算：

```
┌──────────────────────────────────────────────┐
│  ⚙️ 固定经营成本管理             + 添加      │
├──────────────────────────────────────────────┤
│  月度合计：¥6,080                            │
│  未来30天到期：¥6,080                        │
├──────────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐     │
│  │ 🏥 社保公积金                       │     │
│  │ ¥2,100/月                           │     │
│  │ 下次扣款：7月15日                   │     │
│  │ [编辑]  [停用]  [删除]              │     │
│  ├─────────────────────────────────────┤     │
│  │ 🏠 房租                             │     │
│  │ ¥3,500/月                           │     │
│  │ 下次扣款：7月20日                   │     │
│  │ [编辑]  [停用]  [删除]              │     │
│  ├─────────────────────────────────────┤     │
│  │ 💻 Adobe全家桶                      │     │
│  │ ¥480/月                             │     │
│  │ 下次扣款：7月25日                   │     │
│  │ [编辑]  [停用]  [删除]              │     │
│  └─────────────────────────────────────┘     │
└──────────────────────────────────────────────┘
```

**新增固定成本**：

```
┌──────────────────────────────────────────────┐
│  + 添加固定成本                              │
│                                              │
│  名称      [社保公积金]                      │
│  金额      [¥2,100]                          │
│  频率      ● 月付  ○ 季付  ○ 年付            │
│  下次扣款日 [7月15日 ▾]                      │
│  分类      [社保 ▾]                          │
│                                              │
│  [保存]                                      │
└──────────────────────────────────────────────┘
```

***

## 四、数据模型设计

### 4.1 新增 `Receivable` 模型

```swift
@Model
final class Receivable {
    var id: UUID = UUID()
    var clientName: String = ""          // 客户名称
    var projectName: String = ""         // 业务描述
    var amount: Double = 0               // 应收金额
    var expectedDate: Date? = nil        // 预计回款日期
    var status: String = "pending"       // "pending" | "overdue" | "received"
    var receivedDate: Date? = nil        // 实际到账日期
    var note: String = ""
    var createdAt: Date = Date()
    
    var project: Project?
}
```

### 4.2 新增 `FixedCost` 模型

```swift
@Model
final class FixedCost {
    var id: UUID = UUID()
    var name: String = ""                // 名称："社保"、"房租"
    var amount: Double = 0               // 金额
    var frequency: String = "monthly"    // "monthly" | "quarterly" | "yearly"
    var category: String = ""            // "social_insurance" | "rent" | "software" | "other"
    var nextDueDate: Date? = nil         // 下次扣款日
    var isActive: Bool = true
    var createdAt: Date = Date()
    
    var project: Project?
}
```

### 4.3 `Transaction` 模型扩展

```swift
// 新增字段
var cashFlowType: String = "operating"   // "operating" | "personal"
// businessLineTag 不再需要：业务线 = transaction.category（现有字段）
// 看板侧直接对 category 聚合即可得到业务线收入占比，无需新字段
```

### 4.4 `BudgetCategory` 模型扩展

```swift
// 新增字段
var isDirectCost: Bool = true            // 是否为直接成本（默认true，用户可改）
```

### 4.5 `Project` 模型扩展

```swift
// 新增字段
var taxRate: Double = 0                  // 税费预留比例（如 0.2 = 20%）

// 新增关系
@Relationship(deleteRule: .cascade, inverse: \Receivable.project)
var receivables: [Receivable]?

@Relationship(deleteRule: .cascade, inverse: \FixedCost.project)
var fixedCosts: [FixedCost]?
```

### 4.6 新增计算属性

```swift
// === 现金流维度 ===

var availableCash: Double {              // 累计可用资金
    totalIncome - totalSpent
}

var monthlyNetCashFlow: Double {         // 当月净现金流
    monthlyIncome - monthlyExpense
}

var operatingCashFlow: Double {          // 经营现金流
    // 按 cashFlowType == "operating" 筛选 transactions 计算
}

var personalCashFlow: Double {           // 个人现金流
    // 按 cashFlowType == "personal" 筛选 transactions 计算
}

var upcomingFixedCosts: Double {         // 未来30天刚性支出
    let calendar = Calendar.current
    let today = Date()
    let thirtyDaysLater = calendar.date(byAdding: .day, value: 30, to: today)!
    
    return (fixedCosts ?? [])
        .filter { $0.isActive }
        .filter { cost in
            guard let dueDate = cost.nextDueDate else { return false }
            return dueDate >= today && dueDate <= thirtyDaysLater
        }
        .reduce(0) { $0 + $1.amount }
}

// === 收入维度 ===

// 已到账收入 = totalIncome（现有属性，保持不变）

var totalReceivable: Double {            // 待回款总额
    (receivables ?? [])
        .filter { $0.status == "pending" || $0.status == "overdue" }
        .reduce(0) { $0 + $1.amount }
}

var overdueReceivable: Double {          // 逾期金额
    (receivables ?? [])
        .filter { $0.status == "overdue" }
        .reduce(0) { $0 + $1.amount }
}

var overdueRatio: Double {               // 逾期占比
    guard totalReceivable > 0 else { return 0 }
    return overdueReceivable / totalReceivable * 100
}

// === 成本维度 ===

var directCost: Double {                 // 直接成本
    // 按分类的 isDirectCost 属性筛选 transactions 计算
}

var fixedCostMonthly: Double {           // 固定经营成本（月度）
    (fixedCosts ?? [])
        .filter { $0.isActive }
        .reduce(0) { cost in
            switch cost.frequency {
            case "monthly": return $0 + cost.amount
            case "quarterly": return $0 + cost.amount / 3
            case "yearly": return $0 + cost.amount / 12
            default: return $0 + cost.amount
            }
        }
}

var personalCost: Double {               // 个人生活成本
    // 按 cashFlowType == "personal" 筛选 transactions 计算
}

// === 利润维度 ===

var grossProfit: Double {                // 毛利
    totalIncome - directCost
}

var grossMargin: Double {                // 毛利率
    guard totalIncome > 0 else { return 0 }
    return grossProfit / totalIncome * 100
}

var operatingNetProfit: Double {         // 经营净利润
    grossProfit - fixedCostMonthly
}

var taxReserve: Double {                 // 税费预留
    max(0, operatingNetProfit * taxRate)
}

var disposableIncome: Double {           // 个人可支配收入
    operatingNetProfit - personalCost - taxReserve
}
```

***

## 五、完整数据流示意

```
用户记账行为                        系统自动计算
────────────                      ─────────────

收入 ¥5,000                        看板展示
└── 分类：设计外包        ─────→   ├── 累计可用资金 +¥5,000
                                   ├── 已到账收入 +¥5,000
                                   ├── 毛利 +¥5,000
                                   └── 业务线：设计外包 100%

支出 ¥800
└── 分类：素材购买        ─────→   ├── 累计可用资金 -¥800
    （自动归类为直接成本）          ├── 直接成本 +¥800
                                   └── 毛利 -¥800

支出 ¥200
└── 分类：个人餐饮        ─────→   ├── 累计可用资金 -¥200
    （用户标记为个人支出）          └── 个人生活成本 +¥200

═══════════════════════════════════════════════════════

应收账款 ¥8,000（未到账）  ─────→   待回款总额 +¥8,000
                                    已到账收入不变（+¥0）

═══════════════════════════════════════════════════════

固定成本（预设）
├── 社保 ¥2,100/月
├── 房租 ¥3,500/月        ─────→   固定成本合计 ¥6,080/月
└── 软件 ¥480/月                   未来30天刚性支出 ¥6,080

═══════════════════════════════════════════════════════

利润瀑布计算
────────────────────────────────────────────────────────
已到账收入                          ¥5,000
- 直接成本                          ¥800
= 毛利                              ¥4,200    （毛利率 84%）
- 固定经营成本（月度分摊）            ¥6,080
= 经营净利润                        -¥1,880   ⚠️ 本月亏损
- 个人生活成本                      ¥200
- 税费预留 (20%)                    ¥0（亏损时不预留）
= 个人可支配收入                    -¥2,080   ⚠️ 需动用储蓄
```

***

## 六、看板入口设计

### 6.1 在 ProjectDetailView 中的位置

`经营看板` 入口卡片位于 `ProjectDetailView` 滚动页的第 ⑤ 位（详见 `项目预算分类系统和ROI设计.md` § 7.1），替换旧版以时薪/ROI 为核心的经营看板入口：

```
导航栏（返回 + 项目名 + ···菜单）
 ↓
① 项目概览卡片       ← 现有
② 预算分类卡片       ← 现有
③ 分类占比卡片       ← 现有
④ 收支趋势卡片       ← 现有
⑤ 经营看板入口卡片   ← 【本 PRD 定义】仅搞钱模式显示；生活模式此位置仍为「预算防线」入口
⑥ 账单时间轴         ← 现有
```

---

### 6.2 入口卡片：Plus 已解锁态

入口卡展示四个维度各一个核心指标，点击整张卡片 push 到 `ProjectDashboardView`：

```
┌──────────────────────────────────────────────┐
│  📊 经营看板                        查看 ›   │
├──────────────────────────────────────────────┤
│  可用资金            本月净现金流             │
│  ¥28,500            +¥12,300               │
├──────────────────────────────────────────────┤
│  已到账收入          个人可支配               │
│  ¥47,000            ¥18,636                │
│                                              │
│  毛利率 74%  ·  待回款 ¥12,000  ⚠️          │
└──────────────────────────────────────────────┘
```

> 入口卡只展示真实到账数字。待回款以小号辅助信息展示，不与已到账收入并排，避免混淆。

---

### 6.3 入口卡片：免费用户 / Plus 未解锁态

免费用户看到功能说明型锁定卡，不展示任何虚假数字，整张卡点击跳转 `PaywallView`：

```
┌──────────────────────────────────────────────┐
│  📊 经营看板                      Plus  🔒   │
│                                              │
│  算清现金流、真实收入、成本结构和利润，       │
│  搞清楚这个项目到底赚了多少。                │
│                                              │
│  现金流 · 收入确认 · 成本拆分 · 利润瀑布    │
│                                              │
│  [解锁经营看板]                              │
└──────────────────────────────────────────────┘
```

---

### 6.4 经营看板页面结构（`ProjectDashboardView`）

由入口卡 push 进入的独立页面，滚动布局：

```
导航栏（← 返回  项目名称）
  ↓
① 现金流总览卡片
   固定展示「可用资金」+「本月净现金流」，作为最高优先级指标始终在顶部
  ↓
② 四维度 Segmented Control
   [现金流]  [收入]  [成本]  [利润]
  ↓
③ 维度详情卡片
   根据选项动态切换，展示对应维度的完整数据和图表
  ↓
④ 时间周期选择器
   [本月]  [本季]  [本年]
   同时控制③详情卡和⑤趋势图的时间范围
  ↓
⑤ 趋势图表卡片
   根据当前维度自动切换图表类型：
   · 现金流 → 净现金流折线图
   · 收入   → 月度收入柱状图
   · 成本   → 三类成本堆叠图
   · 利润   → 三层利润折线图（毛利 / 净利 / 可支配）
```

***

## 七、直接开发完整版

### 第一阶段：简化版（低门槛）

- 现金流：只展示可用资金和本月净现金流，不拆分经营/个人
- 收入：只展示已到账收入，应收账款功能可选
- 成本：按分类自动判断直接成本/个人成本，固定成本管理可选
- 利润：只展示毛利和经营净利润，不计算可支配收入

### 第二阶段：完整版（深度用户）

- 现金流：支持经营/个人拆分，未来30天刚性支出预估
- 收入：应收账款完整管理，逾期提醒
- 成本：支持手动覆盖成本性质，固定成本管理完整功能
- 利润：三层瀑布完整计算，税费预留设置

***

## 八、付费功能设计
经营看板只有付费用户才能点击进来看


## 九、开发阶段规划

```
Phase 1：数据模型层
├── 新增 Receivable @Model
├── 新增 FixedCost @Model
├── Transaction 扩展 cashFlowType 字段
├── BudgetCategory 扩展 isDirectCost 字段
├── Project 扩展 taxRate、receivables、fixedCosts 字段
└── 计算属性实现

Phase 2：经营看板UI
├── ProjectDashboardView（主看板页面）
├── CashFlowCard（现金流卡片）
├── IncomeCard（收入卡片）
├── CostCard（成本卡片）
├── ProfitCard（利润卡片）
└── 图表组件（折线图、柱状图、环形图、瀑布图）

Phase 3：数据录入与管理
├── ReceivableListView（应收账款管理）
├── FixedCostListView（固定成本管理）
├── TransactionView 扩展（搞钱模式项目下「更多选项」折叠区增加成本性质覆盖，主流程字段不变）
└── 标记到账流程（应收账款 → 收入）

Phase 4：高级功能（Plus）
├── 经营/个人现金流拆分
├── 未来30天刚性支出预估
├── 利润趋势分析
└── AI 经营洞察（可选）
```

***

## 十、关键设计决策

| 决策      | 选择              | 理由                    |
| ------- | --------------- | --------------------- |
| 收入确认方式  | 收付实现制           | 符合普通人心智模型，看板数字真实可靠    |
| 待回款处理   | 独立应收账款模型        | 不污染收入数据，单独管理更清晰       |
| 成本性质判断  | 默认按分类自动判断 + 可覆盖 | 降低记账门槛，大多数情况无需操作      |
| 利润计算方式  | 三层瀑布（毛利→净利→可支配） | 层层递进，看清每个环节的利润情况      |
| 固定成本录入  | 独立管理入口，不通过记账录入  | 一次性配置，避免重复操作          |
| 经营/个人拆分 | 记账时标记，默认为经营     | 搞钱模式项目以经营为主，生活支出需主动标记 |
| 业务线定义   | 业务线 = 收入分类（现有字段），不新增标签字段 | 分类本身已承载业务线语义，看板侧对 `category` 聚合即可，录入侧零摩擦 |
| 记一笔统一性  | 主流程字段完全统一，成本性质折叠在「更多选项」中，由项目模式上下文控制是否出现 | 用户感知到的记账体验与普通模式无差别，搞钱模式的额外数据需求由系统自动处理 |

