# 钱小满 · 项目预算分类系统 PRD

> 版本：V3.1（2026-07-10）
> 覆盖：数据模型 / UI 交互 / AI Prompt / UI结构规划 / Gate策略 / 商业化 / 用户动线
> ⚠️ 经营看板（现金流/收入/成本/利润四维度）已独立至「经营看板PRD.md」

---

## 一、整体设计思路

### 三层完整闭环

```
事前规划              过程执行              事后复盘
────────────         ────────────         ────────────
AI 生成预算分类  →   实时感知记账进度  →   归档生成复盘报告
设置预算周期         分类预警 + IP联动      可分享图片导出
确定项目模式         经营看板（搞钱模式）   AI 经营洞察
```

### 项目模式：搞钱 vs 生活（核心设计决策）

不同项目类型，核心指标完全不同。因此在**创建项目时**引入「项目模式」选择：


| 维度   | 搞钱模式                  | 生活模式                |
| ---- | --------------------- | ------------------- |
| 典型场景 | 外包接单、副业、投资            | 旅行、装修、约会            |
| 核心问题 | 这个项目赚了多少              | 钱花得值不值、有没有超预算       |
| 看板形态 | 经营看板（详见「经营看板PRD.md」） | 预算防线看板（剩余预算 / 日均消费） |
| 时间记录 | 详见「经营看板PRD.md」         | 仅记项目持续天数（参考用）         |


**模式选择逻辑：**

- 创建项目时提供明确的二选一卡片（不默认，不自动判断）
- AI 根据项目名称和描述给出**建议**（灰色提示文字），用户可以自由覆盖
- 创建后可以在编辑页随时更改模式，切换不丢失任何数据

```
┌─────────────────────┐  ┌─────────────────────┐
│  💼 搞钱模式         │  │  🌿 生活模式         │
│  外包 / 副业 / 投资  │  │  旅行 / 装修 / 约会  │
│  算利润和真实时薪    │  │  看预算剩余和性价比  │
│  [选中边框]          │  │                     │
└─────────────────────┘  └─────────────────────┘
AI 建议：根据「新疆之旅」，推荐选「生活模式」
```

---

## 二、数据模型扩展

### 新增 `BudgetItem` 模型

```swift
@Model
final class BudgetItem {
    var id: UUID = UUID()
    var categoryName: String = ""      // "交通"、"餐饮"
    var categoryIcon: String = ""      // SF Symbol name
    var categoryColorHex: String = ""
    var amount: Double = 0             // 该分类分配预算
    var sortOrder: Int = 0
    var alertThreshold: Double = 0     // 0 = 不预警；0.8 = 80% 时提醒（Plus）
    var createdAt: Date = Date()

    var project: Project?
}
```

<!--
### 新增 `TimeEntry` 模型（已迁移至「经营看板PRD.md」）

```swift
@Model
final class TimeEntry {
    var id: UUID = UUID()
    var duration: Double = 0            // 工时数量（小时 or 天）
    var granularity: String = "hour"    // "hour" | "day"
    var rate: Double = 0                // 时薪（hour）或日薪（day）
    var note: String = ""               // 任务描述
    var date: Date = Date()
    var createdAt: Date = Date()

    var project: Project?
}
```

> **为什么支持工日？** 部分场景只能估算天数（如：陪跑这个项目花了我 5 天），无法精确到小时。工时和工日都折算成「时间成本 = duration × rate」，统一计算。
-->

### `Project` 新增字段

```swift
// 项目模式
var projectMode: String = "lifestyle"      // "earning" | "lifestyle"

// 预算相关
var budgetCycle: String = "project"        // "project" | "monthly" | "custom"
var budgetCycleStartDate: Date? = nil
var budgetCycleDays: Int = 30
var budgetAlertThreshold: Double = 0       // 总预算预警线（Plus）

// 搞钱模式专用（详见「经营看板PRD.md」）
var defaultRate: Double = 0                // 默认时薪
var defaultRateGranularity: String = "hour" // "hour" | "day"
var targetIncome: Double = 0               // 目标收入 / 合同金额
var workingDays: Int = 0                   // 实际工作天数（0 = 自动用自然天数）

// 关系
@Relationship(deleteRule: .cascade, inverse: \BudgetItem.project)
var budgetItems: [BudgetItem]?

// TimeEntry 关系已迁移至「经营看板PRD.md」
// @Relationship(deleteRule: .cascade, inverse: \TimeEntry.project)
// var timeEntries: [TimeEntry]?
```

### `Project` 新增计算属性

```swift
// 通用
var budgetItemsAllocated: Double {
    (budgetItems ?? []).reduce(0) { $0 + $1.amount }
}
var budgetUnallocated: Double { budget - budgetItemsAllocated }
var currentCycleSpent: Double { /* 按 budgetCycle 过滤 transactions */ }

// 生活模式
var totalDays: Int {                    // 项目持续天数
    guard let start = createdAt as Date?,
          let txDates = transactions?.map({ $0.date }),
          let lastDate = txDates.max() else { return 1 }
    return max(1, Calendar.current.dateComponents([.day], from: start, to: lastDate).day ?? 1)
}
var dailyAvgSpend: Double {             // 日均花费
    guard totalDays > 0 else { return 0 }
    return totalSpent / Double(totalDays)
}
```

<!--
### 搞钱模式计算属性（已迁移至「经营看板PRD.md」）

```swift
// 搞钱模式·基础层（零门槛）
var netProfit: Double { totalIncome - totalSpent }   // 纯财务 P&L（不含时间成本）
var roi: Double {
    guard totalSpent > 0 else { return 0 }
    return (netProfit / totalSpent) * 100            // 资金回报率
}
var effectiveWorkingDays: Int {          // 有效工作天数
    if workingDays > 0 { return workingDays }
    let days = Calendar.current.dateComponents([.day], from: createdAt, to: Date()).day ?? 0
    return max(1, days)
}
var dailyCost: Double { totalSpent / Double(effectiveWorkingDays) }
var dailyProfit: Double { netProfit / Double(effectiveWorkingDays) }

// 搞钱模式·可选增强层（需用户主动记工时才有意义）
var totalTimeCost: Double {
    (timeEntries ?? []).reduce(0) { $0 + $1.duration * $1.rate }
}
var totalHourEquivalent: Double {        // 统一折算为小时（工日 × 8h）
    (timeEntries ?? []).reduce(0) {
        $0 + ($1.granularity == "day" ? $1.duration * 8 : $1.duration)
    }
}
var hasTimeEntries: Bool { !(timeEntries ?? []).isEmpty }
var effectiveHourlyRate: Double {        // 真实时薪（仅有工时记录时有效）
    guard totalHourEquivalent > 0 else { return 0 }
    return netProfit / totalHourEquivalent
}
```
-->

---

## 三、功能一：AI 项目预算规划

### 3.1 NewProjectView 创建流程改版

**改版后的新建流程（滚动表单，不分步骤）：**

```
① 项目名称（必填）
② 项目描述（选填）
③ 项目模式选择  ← 新增：搞钱 / 生活 二选一，AI 给建议
④ 总预算金额（选填）
⑤ 预算规划区块  ← 新增（有总预算时展开）
⑥ 预算周期      ← 新增
⑦ 预算预警      ← 新增（Plus）
⑧ 图标 / 颜色 / 预览（现有）
```

### 3.2 AI 生成预算交互

**触发方式：** 用户填完名称 + 总预算后，出现按钮区：

```
┌──────────────────────────────────────────────┐
│  💰 预算规划（选填）                          │
│  总预算 ¥5,000                               │
│                                              │
│  [✨ AI 一键生成预算]        [手动添加分类]   │
│                                              │
│  ── 生成前可以补充（选填）──                  │
│  [两人同行，7天，主要自驾...]     文本输入框  │
│  不填也可以直接生成 →                         │
└──────────────────────────────────────────────┘
```

> **为什么用「生成前补充」而不是 Smart Tags？**
> 预设标签覆盖场景有限且易产生干扰，开放文本输入对懒用户无摩擦（直接跳过），对想精准的用户有渠道，更灵活。

**生成后展示：**

```
┌──────────────────────────────────────────────┐
│  🚗 交通    ¥800   ██░░░░░░░░ 16%  [编辑][×]│
│  🍜 餐饮    ¥1000  ███░░░░░░░ 20%  [编辑][×]│
│  🏨 住宿    ¥1500  ████░░░░░░ 30%  [编辑][×]│
│  🎡 娱乐    ¥700   ██░░░░░░░░ 14%  [编辑][×]│
│  + 手动添加分类                              │
│                                              │
│  已分配 ¥4,000  未分配 ¥1,000  ⚠️ 未凑整  │
└──────────────────────────────────────────────┘
```

实时联动：编辑任意一项金额 → 顶部「已分配/未分配」即时更新。

### 3.3 AI 生成预算 Prompt 设计

`LLMService` 新增独立方法：`generateBudgetBreakdown(name:desc:supplement:totalBudget:mode:) async`

```
你是一个预算规划专家。用户正在创建一个「{mode}」类项目，请根据信息生成合理的预算分类明细。

项目名称：{name}
项目描述：{desc}（若为空，根据名字推断）
用户补充：{supplement}（若为空，忽略此项）
总预算：¥{budget}
项目模式：{mode}（搞钱/生活）

要求：
1. 生成 5-8 个分类，覆盖该类项目的主要花销场景
2. 所有分类金额之和 = 总预算（必须严格相等）
3. 比例合理，参考真实消费习惯；金额规模不同比例也不同
4. 使用合适的 SF Symbol 图标名（v5+）和莫兰迪色系 hex
5. 搞钱模式重点考虑：工具/软件、差旅、外包等成本项

返回严格 JSON，无其他内容：
{
  "budget_items": [
    {"name": "交通", "icon": "car.fill", "colorHex": "#A8E0C2", "amount": 800},
    ...
  ],
  "reasoning": "一句话说明分配逻辑"
}
```

### 3.4 预算周期设置

```
┌──────────────────────────────────────────────┐
│  ⏱ 预算周期                                  │
│  ● 整个项目（一次性，不刷新）                 │
│  ○ 按月自动刷新（副业、长期项目适用）         │
│  ○ 自定义天数  [___] 天后刷新                │
└──────────────────────────────────────────────┘
```

**刷新机制：** 不删除任何历史账单，仅通过日期过滤重新计算「当前周期已花费」。

### 3.5 预算预警（Plus）

```
┌──────────────────────────────────────────────┐
│  🔔 预算预警  [Plus]                          │
│  总预算使用率超过 [──●──────] 80% 时提醒      │
│  ○ 各分类单独设置阈值                         │
└──────────────────────────────────────────────┘
```

**IP 情绪联动（核心体验差异化）：**


| 触发状态            | IP 表现 | 推送文案示例                      |
| --------------- | ----- | --------------------------- |
| 某分类超 90%（旅行-餐饮） | 流汗/紧张 | 「餐饮预算快见底了，今晚考虑自己做饭？」        |
| 总预算超 80%（装修）    | 安全帽崩溃 | 「顶住！增项已超预算 15%，快去跟监理对线！」    |
| 总预算仍充裕（旅行中段）    | 墨镜躺平  | 「大交通和住宿锁死在 60%，接下来的每天放心吃喝！」 |
| 项目结束有结余         | 开心/庆祝 | 「这次装修省了 ¥3,200，小满替你鼓掌！」     |


**预警逻辑：**

- Plus 用户：推送通知 + 详情页红色高亮 + IP 情绪状态
- 非 Plus 用户：看到进度条超线变红，但推送和 IP 联动被虚化遮罩，引导升级

### 3.6 ProjectDetailView 预算进度展示

```
┌──────────────────────────────────────────────┐
│  💰 预算进度  本月（4月）                     │
│  ████████░░  ¥3,200 / ¥5,000   64%          │
│                                              │
│  🚗 交通   ██████░░░░  ¥480/¥800   60%      │
│  🍜 餐饮   █████████░  ¥940/¥1000  94% ⚠️   │
│  🏨 住宿   ████░░░░░░  ¥600/¥1500  40%      │
│  🎡 娱乐   ██░░░░░░░░  ¥140/¥700   20%      │
│                                              │
│  未分类支出 ¥1,040（无对应预算分类）          │
└──────────────────────────────────────────────┘
```

**超预算自动高亮：** 当某分类实际支出超过分配额，超出部分自动标红显示为「超出 ¥XXX」，无需用户手动标记「增项」。

---

## 四、功能二：经营看板（搞钱模式）

> ⚠️ **已独立为「经营看板PRD.md」**
>
> 经营看板已重新设计为四维度分析（现金流/收入/成本/利润），详见独立文档。

---

## 五、功能三：预算防线看板（生活模式）

> 生活类项目（旅行、装修、约会等）没有「收入」概念，ROI 不适用。核心需求是：**花了多少、有没有超标、性价比如何。**

### 5.1 看板 UI（`ProjectLifestyleView`）

```
┌──────────────────────────────────────────────┐
│  🌿 预算防线  ·  新疆之旅                    │
├──────────────────────────────────────────────┤
│  预算剩余（核心指标）                         │
│                                              │
│         ¥1,800 剩余                          │
│  ████████████░░░░  ¥8,200 / ¥10,000  82%   │
│  [进度条自动变色：绿→黄→红]                  │
│                                              │
│  [IP 情绪联动区域]                           │
│  🦫 「大交通和住宿锁死在55%，               │
│     接下来每天可以放心吃喝玩！」              │
├──────────────────────────────────────────────┤
│  旅行大件 vs 日常消费（旅行专属视图）        │
│  大件（机票+住宿）   ¥5,500  67%            │
│  日常消费（去掉大件） ¥2,700  33%            │
│  日均自由消费  ¥385/天（7天）                │
├──────────────────────────────────────────────┤
│  分类占比  [环形图]                           │
│  🚗 行      ¥2,200  27%                     │
│  🏨 住      ¥3,300  40%                     │
│  🍜 食      ¥1,400  17%                     │
│  🎡 游      ¥1,300  16%                     │
├──────────────────────────────────────────────┤
│  每日消费走势  [折线图]                       │
│  哪天花最多一眼看出（如景区日 ¥890）         │
└──────────────────────────────────────────────┘
```

**旅行大件锁死逻辑：** 「行」「住」分类自动识别为大件，在看板中独立展示，计算剩余日均消费时可单独观察。

### 5.2 三类生活场景的 IP 点评差异

通过项目名称/描述 AI 推断场景，生成对应情境化文案：

**旅行中途进度良好：**

> 「大交通和住宿锁死在 60% 以内，接下来的每一天，放心吃喝！」

**装修出现超支：**

> 「顶住！增项已经超预算 15% 了，快去跟监理对线！」

**约会项目归档：**

> 「这 200 天里你们一起喝了 64 杯奶茶。感情的代价，甜的。」

**装修超预算自动识别：** 无需用户手动标记「增项」，当某分类实际支出超出预算分配额时，超出部分自动用醒目颜色标注，一眼可见超支来自哪里。

---

## 六、功能四：项目复盘（归档时触发）

> 这是整个方案的**高光功能**。用户归档项目时不是简单消失，而是触发一次有仪式感的总结。

### 6.1 触发时机

用户点击「归档项目」→ 系统弹出复盘 Sheet，不是直接归档。

### 6.2 复盘报告内容

<!--
**搞钱模式复盘（`ProjectEarningReview`）（已迁移至「经营看板PRD.md」）：**

```
《品牌设计外包 · 经营复盘》

💰 收支结果
  总收入  ¥12,000  /  目标 ¥15,000（80%）
  总成本  ¥8,500（时间成本 ¥4,050 + 支出 ¥4,450）
  净利润  ¥3,500  ROI 41.2%

⏱️ 时间投入
  累计 40.5 小时，真实时薪 ¥87.5/h

📊 最大三笔支出
  ① 设计素材  ¥1,200
  ② 差旅      ¥1,200
  ③ 软件工具  ¥800

🤖 AI 总结
  这个项目盈利稳健，但工时超预估 16%，
  拉低了真实时薪。下次接同类项目，建议
  报价提高 15%，或控制沟通轮次。

💡 下次预算建议（基于本次数据）
  推荐预算：¥9,000（本次实际成本 × 1.05 缓冲）
  推荐报价：¥13,800（维持 60% 利润率）
```
-->

**生活模式复盘（`ProjectLifestyleReview`）：**

```
《新疆之旅 · 旅行账单》

💰 总花费  ¥9,200  /  预算 ¥10,000（节省 ¥800）
📅 持续天数  8 天
👤 日均花费  ¥1,150/天

🏆 控制最好的分类
  住宿  预算 ¥3,000  实际 ¥2,400（省 ¥600）

📈 最大超支分类
  餐饮  预算 ¥1,000  实际 ¥1,380（超 38%）

🤖 AI 总结
  这次旅行预算执行得不错，住宿控制尤其出色。
  餐饮略超，可能是当地特色美食没抵住诱惑——
  值的。

💡 下次同类旅行建议预算
  8 天新疆  参考 ¥9,500（基于本次实际）
```

### 6.3 可分享图片导出（Plus 核心付费点）

**功能：** 将复盘报告渲染为精美长图，用户可分享到小红书 / 朋友圈。

**技术实现：** `ImageRenderer`（iOS 16+）将 SwiftUI `ProjectReviewCard` 渲染为 `UIImage`，支持保存到相册。

**营销价值：** 用户自发传播——「我的阿勒泰 10 日穷游账单」「9 万搞定原木风公寓清单」，天然带量。

**权限设计：**

- 复盘基础数字（收支汇总、最大超支分类）：所有用户
- AI 总结内容：Plus 用户（遮罩 + 升级引导）
- 导出精美长图：Plus 用户（按钮锁定）
- 下次预算建议：Plus 用户（遮罩 + 升级引导）

---

## 七、ProjectDetailView UI 结构规划

### 7.1 整体卡片顺序

不引入 Tab 导航，保持现有滚动卡片范式，在现有卡片序列中**插入两张新卡片**：

```
导航栏（返回 + 项目名 + ···菜单）
 ↓
① 项目概览卡片       ← 现有：图标 + 描述 + 三格指标 + 总预算进度条
② 预算分类卡片       ← 【新增】内联展示，不跳转，管理操作用 Sheet
③ 分类占比卡片       ← 现有：donut 图
④ 收支趋势卡片       ← 现有：area 折线图
⑤ 看板入口卡片       ← 【新增】紧凑数字预览，点击 push 到独立看板页
⑥ 账单时间轴         ← 现有：按日分组的账单列表
```

**设计原则：** ② 预算分类作为「过程数据」紧跟概览卡片，让用户一眼看到钱花在哪里；⑤ 看板入口放在账单列表前、趋势图后，作为「分析层」的入口，不干扰日常记账流。

---

### 7.2 ② 预算分类卡片（内联，不跳转）

与现有分类占比、收支趋势卡片**完全相同的容器样式**（`cornerRadius: 32`、`cardBackground`、轻阴影）：

**已配置预算分类时：**

```
┌──────────────────────────────────────────────┐
│  💰 预算分类                    管理分类 ›   │  ← 右侧灰色小字 → 打开 Sheet
│                                              │
│  总进度  ██████████░░░░  ¥8,200 / ¥10,000  │  ← 复用现有进度条颜色逻辑
│                                              │
│  🚗 交通  ██████░░░░  ¥480/¥800    60%      │
│  🍜 餐饮  ████████░░  ¥940/¥1000   94% ⚠️  │  ← 超警线分类红色高亮
│  🏨 住宿  ████░░░░░░  ¥600/¥1500   40%      │
│  + 3 个分类...  展开 ›                        │  ← 超 3 个自动折叠
└──────────────────────────────────────────────┘
```

「管理分类 ›」打开 `BudgetManagementSheet`（半屏 Sheet），在里面增删改分类金额和预警阈值，不做页面跳转。

**未配置预算分类时（空状态）：**

```
┌──────────────────────────────────────────────┐
│  💰 预算规划                                  │
│                                              │
│  🦫  还没有设置预算分类                       │  ← 复用现有空状态样式
│      [✨ AI 生成  🔒 Plus]   [手动添加]       │
└──────────────────────────────────────────────┘
```

> 当项目未设置总预算（`budget == 0`）时，整张预算分类卡片不显示，保持详情页简洁。

---

### 7.3 ⑤ 看板入口卡片（紧凑预览 → Push 独立页面）

作为全功能看板的「入口钩子」，展示 2-3 个核心数字，点击整张卡片 `NavigationLink push` 到独立页面。

<!--
**搞钱模式（`ProjectEarningView`）- 已有时间记录时（已迁移至「经营看板PRD.md」）：**

```
┌──────────────────────────────────────────────┐
│  💼 经营看板                        查看 ›   │
│                                              │
│  真实时薪     净利润     ROI                 │
│  ¥87.5/h    ¥3,500    41.2%                │
│                                              │
│  工时 40.5h  |  目标进度  ████████░░  80%   │
└──────────────────────────────────────────────┘
```

**搞钱模式 - 尚无时间记录 / 未开启（锁定态）：**

```
┌──────────────────────────────────────────────┐
│  💼 经营看板                        Plus 🔒  │
│                                              │
│  追踪工时与真实时薪，看清这个项目值不值做。  │
│  真实时薪 · 净利润 · ROI · 月趋势分析       │
│                                              │
│  [解锁经营看板]                              │
└──────────────────────────────────────────────┘
```
-->

**生活模式（`ProjectLifestyleView`）- 免费用户基础版：**

```
┌──────────────────────────────────────────────┐
│  🌿 预算防线                        查看 ›   │
│                                              │
│  剩余预算 ¥1,800    已用 82%                │
│  ████████████████░░░░                        │
└──────────────────────────────────────────────┘
```

---

### 7.4 ··· 菜单扩展

现有菜单新增「归档并复盘」替换「归档项目」，触发复盘流程：

```swift
// 原有
Button { store.toggleArchive(project: project) } label: {
    Label("归档项目", systemImage: "archivebox")
}

// 改为
Button { showArchiveAndReview = true } label: {
    Label("归档并复盘", systemImage: "archivebox.fill")
}
// → 弹出 ProjectReviewSheet，用户看完复盘后点「确认归档」
```

---

### 7.5 风格统一规则

基于现有代码，新组件严格复用以下已有元素：

| 视觉元素 | 现有实现来源 | 新组件沿用方式 |
|---|---|---|
| 卡片容器 | `.background(cardBackground)` + `cornerRadius(32)` + `shadow(0.03, r:15)` | 预算分类卡、看板入口卡完全一致 |
| 强调色 | `progressColorPair(for: project.colorHex)` | 分类进度条、看板卡片色块用同一函数 |
| 节标题 | `.font(.heavy, 20)` + `.textBlack` | 「预算分类」「经营看板」同款字重 |
| 右侧跳转小字 | `"支出明细"` / `"按月汇总"` 的灰色 `.caption` | `"管理分类"` / `"查看"` 同款 |
| 空状态 | `Text("🦫") + Text("暂无...").gray` | 未设置预算时完全复用 |
| 进度条 | 现有 budget progress bar 样式 | 分类进度条按比例缩小复用 |
| 数字字号 | 三格指标 `.title2.heavy` | 看板入口卡核心数字同款 |
| Plus 锁标 | `PaywallView` / `FuelPackSheet` 既有样式 | 统一用 SF Symbol `lock.fill` + `"Plus"` 文字 |

---

## 八、付费功能 Gate 策略

> **核心原则：只对「存在真实数据」的内容做遮罩；对「需要先录入才有内容」的功能做锁定入口。遮罩假数据显得欺骗，会伤害信任。**

### 8.1 AI 一键生成预算 → 按钮锁定，点击触发付费墙

按钮本身就是入口，没有内容可以遮罩。在按钮上展示 Plus 标记，点击弹出 `PaywallView`。「手动添加」对免费用户始终可用。

```
[✨ AI 一键生成  🔒 Plus]    [手动添加]
```

免费用户可以完整使用手动设置预算分类，感知到 AI 生成是更高效的路径。

---

### 8.2 经营看板（搞钱模式）

> ⚠️ **已迁移至「经营看板PRD.md」**

---

### 8.3 预算防线看板完整版（生活模式）→ 基础版免费 + 高级区块遮罩

生活模式基础预算进度对免费用户完整可用，入口卡和基础数字都可见。进入完整看板页后，高级分析区块（日均消费、大件分析、IP 点评）因为有**真实数据**，可以做遮罩。

**完整看板页内（免费用户）：**

```
总预算进度条  ¥8,200 / ¥10,000  82%      ← 完整显示（免费）

┌──────────────────────────────────────┐
│  每日消费分析          Plus 🔒        │  ← 模糊遮罩
│  日均 ¥▓▓▓/天  大件占 ▓▓%            │
│  [升级查看完整分析]                   │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│  IP 点评               Plus 🔒        │  ← 遮罩
│  🦫 ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓          │
└──────────────────────────────────────┘
```

---

### 8.4 项目复盘 → 基础数字免费展示 + AI 洞察遮罩 + 导出锁定

复盘基于**真实账单数据**，账单汇总对所有用户可见——真实数字本身就是最强的付费转化钩子。AI 总结和导出是差异化的 Plus 内容。

```
《新疆之旅 · 旅行账单》

总花费 ¥9,200  /  预算 ¥10,000（省 ¥800）   ← 免费显示
持续 8 天  日均 ¥1,150/天                     ← 免费显示
最大超支：餐饮 超出 38%                        ← 免费显示

┌──────────────────────────────────────┐
│  🤖 AI 总结              Plus 🔒     │  ← 内容真实但模糊
│  这次旅行预算执行得不▓▓▓▓▓▓▓        │
│  餐饮略超，可能是▓▓▓▓▓▓▓▓▓▓        │
│  [升级解锁 AI 总结 + 下次建议]       │
└──────────────────────────────────────┘

[导出精美图片  🔒 Plus]    [确认归档]     ← 导出按钮锁定，点击触发付费墙
```

**免费用户归档体验：** 看到真实收支汇总数字 → 被遮罩的 AI 总结引发好奇 → 升级动机来自「想要更多分析」而非「猜测遮罩后是什么」，转化更自然。

---

## 九、权限与商业化汇总

| 功能 | 免费版 | Plus |
|---|---|---|
| 预算分类手动设置 | ✅ | ✅ |
| AI 一键生成预算 | ❌（按钮锁定，点击触发付费墙） | ✅ |
| 预算进度查看 | ✅ | ✅ |
| 预算预警通知 + IP 情绪联动 | ❌ | ✅ |
| 经营看板（搞钱模式） | 详见「经营看板PRD.md」 | 详见「经营看板PRD.md」 |
| 预算防线看板基础版（生活模式） | ✅（总进度 + 分类占比） | ✅ |
| 预算防线看板完整版（日均/大件分析/IP点评） | ❌（高级区块遮罩） | ✅ |
| 项目复盘基础数字（收支汇总） | ✅ | ✅ |
| 复盘 AI 总结 + 下次预算建议 | ❌（内容遮罩） | ✅ |
| 复盘导出精美长图 | ❌（按钮锁定） | ✅ |

---

## 十、用户完整动线

### 场景 A：旅行项目（生活模式）

1. 新建项目「新疆之旅」，AI 建议「生活模式」→ 用户确认
2. 填写总预算 ¥10,000 → 点「AI 一键生成预算」
3. 补充：「8天，两人，自驾，主要玩景区」→ 生成分类
4. 微调住宿 ¥3000 → 旅游 ¥1500，确认
5. 设置预算周期：整个项目
6. 旅途中每天记账，看板实时更新进度条
7. 餐饮快用完时，IP 流汗推送提醒
8. 旅行结束，归档 → 系统展示复盘报告
9. Plus 用户导出精美图片发小红书

### 场景 B：外包项目（搞钱模式）

> ⚠️ **详见「经营看板PRD.md」**

---

## 十一、开发阶段规划

```
Phase 1：基础模型层（无 UI）
├── 新增 BudgetItem @Model
├── Project 新增字段（SwiftData Migration V6）
├── AppStore CRUD 方法扩展
└── 计算属性实现

Phase 2：预算规划功能
├── NewProjectView 模式选择 + 预算规划区块
├── LLMService.generateBudgetBreakdown()
├── ProjectDetailView 预算进度卡片
├── 预算周期刷新逻辑
└── 预算预警 + IP 情绪联动 + 本地通知（Plus）

Phase 3：防线看板（生活模式）
├── ProjectLifestyleView（生活模式看板）
└── LLMService.generateLifestyleInsight()

Phase 4：项目复盘
├── ProjectReviewView（生活模式复盘报告）
├── 归档流程改版（复盘 Sheet 拦截）
├── LLMService.generateProjectReview()
└── ImageRenderer 导出精美长图（Plus）
```

> ⚠️ **经营看板开发规划详见「经营看板PRD.md」**

**关键技术注意事项：**

- SwiftData + CloudKit Migration V6 需要谨慎处理 `BudgetItem` 的 `deleteRule: .cascade` 方向
- AI 预算生成、复盘均用**独立方法**，不污染现有记账解析管道（`parseTransaction` 不变）

---

## 十二、项目类型智能配置系统 (2026-07-13)

### 12.1 背景与目标

**问题**：生活模式下的"大件 vs 日常"分析功能存在硬编码问题：
- 大件关键词只匹配旅行场景的交通/住宿（如机票、酒店）
- 文案和对照标准写死为旅行场景（如"特种兵穷游"、"¥800/天 = 贵妇级别"）
- 对于其他类型的项目（如新房装修、婚礼、买车等），分析结果不准确且文案不匹配

**目标**：实现项目类型智能配置系统，根据项目名称/描述自动识别项目类型，动态返回对应的大件关键词、文案和对照标准，覆盖31个常见生活场景。

### 12.2 项目类型覆盖（31个场景）

| 类别 | 项目类型 | 识别关键词 | 大件分类 | 对照标准 |
|------|---------|-----------|---------|---------|
| **旅行出行** | 旅行/旅游 | 旅行、旅游、度假 | 交通+住宿 | ¥800/天 = 贵妇级别 |
| | 出差 | 出差、公务、商务 | 交通+住宿 | ¥500/天 = 商务标准 |
| | 自驾游 | 自驾、公路旅行 | 油费+住宿 | ¥600/天 = 豪华自驾 |
| | 露营 | 露营、野营、户外 | 装备采购 | ¥500/次 = 豪华露营 |
| | 滑雪 | 滑雪、冬运、雪场 | 雪票+装备 | ¥1000/天 = 高端滑雪 |
| **家居装修** | 新房装修 | 装修、新房、毛坯 | 家具+家电 | ¥2000/㎡ = 豪装标准 |
| | 旧房翻新 | 翻新、改造、老房 | 家具+建材 | ¥5000/项 = 全面翻新 |
| | 搬家 | 搬家、乔迁 | 搬家费+新家具 | ¥3000/次 = 品质搬家 |
| | 家具采购 | 家具、沙发、床 | 主要家具 | ¥2000/件 = 品质家居 |
| | 家电采购 | 家电、电器、空调 | 主要家电 | ¥3000/件 = 智能家电 |
| **婚礼庆典** | 婚礼 | 婚礼、结婚、婚庆 | 婚庆+酒店 | ¥100000/场 = 豪华婚礼 |
| | 满月酒 | 满月、百日、周岁 | 酒席+布置 | ¥20000/场 = 隆重举办 |
| | 生日派对 | 生日、派对 | 场地+蛋糕 | ¥5000/场 = 盛大派对 |
| | 毕业典礼 | 毕业、毕业季 | 学士服+摄影 | ¥1000/人 = 精彩记录 |
| **教育学习** | 留学 | 留学、出国、海外 | 学费+住宿 | ¥10000/月 = 品质留学 |
| | 考试备考 | 考研、考公、考编 | 课程+教材 | ¥3000/月 = 高端辅导 |
| | 培训学习 | 培训、课程、技能 | 学费+教材 | ¥5000/期 = 高端培训 |
| | 驾照学习 | 驾照、驾校、学车 | 学费+考试费 | ¥8000/次 = VIP班 |
| **健康医疗** | 体检医疗 | 体检、医疗、看病 | 检查+治疗 | ¥5000/次 = 高端医疗 |
| | 牙齿矫正 | 牙齿、矫正、正畸 | 矫正+种植 | ¥30000/次 = 隐形矫正 |
| | 医美 | 医美、美容、整形 | 手术+注射 | ¥10000/次 = 高端医美 |
| | 健身 | 健身、运动、减肥 | 年卡+私教 | ¥2000/月 = 私教健身 |
| **宠物** | 宠物 | 宠物、猫、狗 | 医疗+粮食 | ¥1000/月 = 品质养宠 |
| **汽车** | 买车 | 买车、购车、提车 | 车款+税费 | ¥200000/辆 = 豪华座驾 |
| | 汽车保养 | 保养、维修、修车 | 机油+轮胎 | ¥2000/次 = 深度保养 |
| **数码电子** | 电脑组装 | 电脑、组装、装机 | 核心硬件 | ¥10000/台 = 旗舰配置 |
| **投资理财** | 投资理财 | 投资、理财、股票 | 本金+大额 | ¥10000/笔 = 高端投资 |
| **节日购物** | 年货采购 | 年货、春节、过年 | 礼品+食材 | ¥5000/户 = 丰盛年货 |
| | 购物节 | 双十一、618、大促 | 数码+家电 | ¥500/件 = 疯狂购物 |
| **社交** | 社交聚会 | 聚会、聚餐、团建 | 场地+餐饮 | ¥500/次 = 精致聚会 |
| **默认** | 日常开销 | 日常、生活 | 交通+住宿 | ¥300/天 = 品质生活 |

### 12.3 技术实现

**核心文件**：`Models/ProjectTypeConfig.swift`

```swift
// 项目类型枚举
enum ProjectType: String, CaseIterable, Identifiable {
    case travel, businessTrip, roadTrip, camping, skiing
    case renovation, remodeling, moving, furniture, appliance
    case wedding, babyShower, birthday, graduation
    case studyAbroad, examPrep, training, drivingSchool
    case medical, dental, cosmetic, fitness, pet
    case carPurchase, carMaintenance, computerBuild
    case investment, newYearShopping, shoppingFestival
    case social, daily
}

// 项目类型配置结构
struct ProjectTypeConfig {
    let type: ProjectType
    let displayName: String        // 显示名称
    let icon: String              // SF Symbol
    let keywords: [String]        // 识别关键词
    let bigItemKeywords: [String] // 大件分类关键词
    let bigItemLabel: String      // 大件显示文案
    let dailyLabel: String        // 日常显示文案
    let description: String       // 功能描述
    let referenceStandard: ReferenceStandard // 对照标准
}

// 对照标准
struct ReferenceStandard {
    let lowLabel: String          // 低端标签
    let highLabel: String         // 高端标签
    let unit: String              // 单位
    let threshold: Double         // 高端阈值
    let format: (Double) -> String // 格式化函数
}
```

**智能识别逻辑**：
```swift
// 按优先级匹配（越具体的越靠前）
let priorityOrder: [ProjectType] = [
    .wedding, .babyShower, .graduation, .studyAbroad,
    .renovation, .remodeling, .skiing, .camping,
    .roadTrip, .businessTrip, .travel,
    // ... 其他类型
    .social, .daily  // 默认
]

// 遍历优先级列表，匹配关键词
for type in priorityOrder {
    let matched = config.keywords.filter { text.contains($0) }
    if !matched.isEmpty { return type }
}
return .daily  // 默认返回日常开销
```

**使用方式**（在 `ProjectLifestyleView` 中）：
```swift
// 获取项目类型配置
let config = ProjectTypeConfigManager.shared.getConfig(
    name: project.name,
    description: project.desc
)

// 使用动态配置
Text(config.bigItemLabel)  // "大件（家具+家电）"
Text(config.description)   // "剔除家具家电，看建材辅材和人工费花了多少"
Text(config.referenceStandard.format(dailyFree))  // "¥2000/㎡ = 豪装标准"
```

### 12.4 涉及文件

| 文件 | 修改内容 |
|------|---------|
| `Models/ProjectTypeConfig.swift` | 新增项目类型智能配置系统，包含31个项目类型的完整配置（关键词、大件分类、文案、对照标准）|
| `Views/ProjectLifestyleView.swift` | 集成智能配置系统，大件识别使用动态关键词，文案和对照标准根据项目类型动态显示 |
