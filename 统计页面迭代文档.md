# 财务统计页面迭代文档 (v2)

## 1. 目标
将财务统计页面升级为高保真 UI 设计，强化“全局宏观”加“项目微观”的分析能力，提升用户的记账成就感与财务健康感知。

## 2. 核心功能与 UI 升级

### 2.1 顶部导航与时间控件
- **Header**: 右侧增加通知铃铛 Icon。
- **日期显示**: 移除左右切换箭头，改为纯文本居中显示当前周期（如 `2/27 - 5/28 v`），点击后呼出对应的时间筛选面板。

### 2.2 顶部 IP 状态卡片（新增）
- **UI展示**: 左侧展示"头上长橘子/喝咖啡的水豚(小满)" IP 形象，右侧展示健康分仪表盘。
- **健康分 (Health Score) 算法（权重组合）**: 
  满分 100 分，最低 0 分。由两部分组成：
  - **静态基本面（60分）**: 基于收支比（支出/收入）的阶梯扣分，衡量当期收支平衡状态。
    - 收支比 <= 1：拿满 60 分。
    - 1 < 收支比 <= 1.2（轻度赤字）：扣 10 分，得 50 分。
    - 1.2 < 收支比 <= 1.5（中度赤字）：扣 20 分，得 40 分。
    - 收支比 > 1.5（严重赤字）：扣 40 分，得 20 分。
    - **边界情况**: 若收入为 0 但有支出（资产消耗状态），固定得 30 分。
  - **动态趋势面（40分）**: 初始 20 分，根据环比变化加扣分。
    - 比上期节省：加 `(上期支出 - 当期支出) / 上期支出 * 20` 分（最多 +20，上限 40 分）。
    - 比上期多花：扣 `(当期支出 - 上期支出) / 上期支出 * 20` 分（最多 -20，下限 0 分）。
  - **预算扣分**: （已移除）因全局月度统计与项目跨月预算粒度不匹配，预算超支监控移至单独的"项目详情页"处理。
  - **得分等级**:
    - 80-100 分：财务状况优秀，收支平衡且趋势向好（绿色）
    - 60-79 分：财务基本健康，但仍有改善空间（橙色）
    - <60 分：财务状况告急，需要调整（红色）
- **环比文案**: "比上期节省了 ¥XXX" 或 "比上期多花了 ¥XXX"。
  - **上期定义**:
    - 周：上周
    - 月：上个月
    - 年：上一年
    - 自定义：往前推与自定义天数相同的周期（例如自定义选了 15 天，上期就是开始日期往前推 15 天）。

### 2.3 支出占比卡片重构
- **版式**: 由上下结构改为左右结构（左侧环形图，右侧图例列表）。
- **图例强化**: 图例列表中增加对应分类/项目的 Icon，以及底部的占比进度条（Progress Bar）。
- **多维度切换**: 保留“按项目”、“全部分类”、“项目内分类”三个维度的 Segmented Control。

### 2.4 区间收支趋势（图表升级）
- **面积图**: 将原本的单线条折线图升级为下方带有渐变色填充的面积图（Area Chart）。
- **峰值提示**: 在图表中找出当期支出的最高点，绘制高亮气泡框（如“5/8 支出峰值 ¥222”）。
- **UI 细节**: 标题右侧增加 `>` 箭头指示。

### 2.5 底部卡片（本月小结）
- **版式**: 移除了原有的“预算健康度”卡片，由“本月小结”卡片独享底部全宽空间，避免跨月项目预算统计在此处引发歧义。
- **本月小结（标题固定）**:
  - 本月共支出 X 笔
  - 平均每天支出 ¥X
  - 最高一笔支出 ¥X
  - 累计记账 X 天

### 2.6 豚言豚语
- 保持在页面最底部，维持现有样式，提供基于数据的 AI 洞察。

## 3. 实施步骤
1. **素材准备**: 将提供的 `IP.png` 导入 `Assets.xcassets`。
2. **逻辑实现**: 
   - 增加健康分和环比算法。
   - 实现“本月小结”的统计逻辑。
3. **UI 还原**: 
   - 替换顶部筛选 UI。
   - 开发 `IPStatusCard`。
   - 重构 `DonutChartCard` 和 `DonutLegendView`。
   - 升级 `LineChartView` 为带有面积和峰值提示的 `AreaChartView`。
   - 实现底部的本月小结全宽卡片，移除全局维度的预算健康度模块。