这个问题比导出难得多，而且钱小满特有的**项目制**让它更复杂。让我来拆解清楚。

---

## 导入难在哪里：两层问题

导出是"有结构 → 无结构"，简单。导入是"无结构 → 有结构"，难。而且有**两层**不同性质的难题：

### 第一层：格式层（技术问题，可解）

各大 App 导出的 CSV 列名五花八门：

| App | 典型列格式 |
|-----|----------|
| **随手记** | 记账时间 / 类型 / 金额 / 一级分类 / 二级分类 / 账户 / 商家 / **项目** / 备注 |
| **MOZE** | Date / Type / Category / Subcategory / Amount / Note / Account / Currency |
| **MoneyMoney** | Date / Amount / Name / Payee / Category / Note |
| **钱迹** | 日期 / 收支 / 金额 / 分类 / 子分类 / 账户 / 备注 |
| **通用导出** | 完全不确定 |

这层问题可以靠**格式预设 + 列映射 UI** 解决，有固定解法。

### 第二层：语义层（设计问题，真正的难点）

**这才是钱小满面临的核心矛盾：**

其他 App 是**平铺流水**结构（所有账在一个大池子里），钱小满是**项目制**结构（账需要归属到某个抽屉）。

```
随手记数据：
2024-03 餐饮 58元
2024-03 交通 12元
2024-03 购物 230元  ← 这些记录"属于"哪个项目？系统不知道

钱小满需要：
├── 项目：新疆旅行
│   └── 2024-03 餐饮 58元（旅行途中）
├── 项目：日常开销
│   └── 2024-03 交通 12元
└── 项目：装修
    └── 2024-03 购物 230元（买建材）
```

**用户在迁移时面对的现实是：** 他们在旧 App 里压根就没有区分过这些账属于哪个"项目"，项目制是钱小满独有的概念，历史数据天然缺少这个维度。

---

## 正确的解题思路：不要强行解决语义层

### 错误做法 ❌
试图在导入时自动推断每条记录属于哪个项目（规则引擎或简单 AI 推断）。结果：错误率高，用户反而不信任，还不如不做。

### 正确做法 ✅：两步走，分开处理两层问题

**第一步（导入时）：只解决格式层，语义层暂缓**

把所有导入的数据放进一个特殊的**"导入数据"虚拟项目**，或者叫"待整理"：

```
导入完成 ✓
├── 1,247 条记录已导入
└── 全部暂存于「历史账单」项目
    （点击「整理」把它们分配到不同项目）
```

这样用户的数据是完整的，分类、金额、备注、日期全都在，一条都不丢。

**第二步（导入后）：用 AI 辅助批量整理，这才是钱小满的差异化**

这一步完全可以接入 LLM Service，做一个"AI 智能整理"功能：

```
小满：我帮你分析了导入的 1,247 条历史数据。
     根据日期和分类，我发现了几个规律：

     ① 2023-07 ~ 2023-08 的餐饮/交通集中在外地商户
       → 可能是一次旅行？建议创建「2023暑假旅行」项目

     ② 2024-01 ~ 2024-03 有多笔「家居」「建材」消费
       → 建议归入「装修」项目

     ③ 其余日常消费直接归入「日常开销」

     是否按这个方案整理？可以调整后再确认。
```

钱小满本身就有 AI 对话 + 记忆规则系统，这个"AI 整理助手"天然适配现有架构，不是额外开发一套东西。

---

## 实际的导入流程设计

```
第 1 屏：选择来源
┌────────────────────────────────┐
│  📥 导入账单                    │
│                                │
│  选择来源 App                  │
│  ┌──────┐ ┌──────┐ ┌──────┐  │
│  │随手记│ │MOZE  │ │钱迹  │  │
│  └──────┘ └──────┘ └──────┘  │
│  ┌──────────────────────────┐  │
│  │  通用 CSV（手动映射列）  │  │
│  └──────────────────────────┘  │
└────────────────────────────────┘

第 2 屏：列映射确认（通用 CSV 才需要）
  预览前 3 行数据，让用户确认：
  "日期" → date ✓
  "金额" → amount ✓  
  "收支" → type ✓
  "分类" → categoryName ✓
  "备注" → note ✓
  [忽略其余列]

第 3 屏：导入结果
  ✅ 已导入 1,247 条记录
  📁 暂存于「历史账单」项目
  
  [稍后整理]   [现在用 AI 整理 ✨]
```

---

## 关于随手记的特殊情况

值得注意的是，随手记本身也有"项目"字段。如果用户从随手记导入，**可以直接映射项目字段**，不需要 AI 推断这一步。这是最理想的情况，优先支持。

---

## 开发优先级重排

| 功能 | 优先级 | 复杂度 | 说明 |
|-----|--------|--------|------|
| **CSV 导出** | 🔴 高 | 低 | V2.1，先做 |
| **CSV 导入（格式层）** | 🟠 中 | 中 | V2.2，支持随手记/MOZE预设 + 通用列映射 |
| **AI 整理助手（语义层）** | 🟡 中 | 中 | V3.0，接 LLMService，复用现有 AI 能力 |
| **完整双向迁移** | 🟡 低 | 高 | V3.0 后期 |

---

## 一个值得认真对待的细节

**导入要有撤销机制。** 用户导入了 1000 条历史数据发现不对，不能让他们一条一条删。需要支持"撤销本次导入"（按 `createdAt` 时间戳批量删除同一批导入的数据）。这在 SwiftData 里实现很简单：给导入的 Transaction 打一个 `importBatchID: UUID?` 标记即可。

---

**总结一句话：导入的格式层不难，真正的难题是项目映射的语义层，而这个恰好是钱小满的 AI 能力最擅长的事。不要试图在导入时一次解决，分两步走反而体验更好。**