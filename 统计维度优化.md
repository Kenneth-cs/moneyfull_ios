我们来一步步拆解并设计这个更精细化的财务统计板块。既然“钱小满”的核心差异点是**项目制**，那么统计报表的设计就必须紧紧围绕“全局宏观”与“项目微观”的结合来做。

### 1. 解决月份选择只能选到“日”的问题

**原因分析**：
在 `AnalyticsView.swift` 中，弹窗使用的是 `DatePicker(..., displayedComponents: [.date]).datePickerStyle(.graphical)`。这是 iOS 原生的日历组件，默认一定会展示到具体的某一天。

**优化方案**：
SwiftUI 原生没有单纯只选“年月”的优雅组件。为了体验更好，我们可以自定义一个“年月选择器”（MonthYearPicker）：
*   **UI 表现**：弹窗出现后，左侧是“年份”滚轮（2023, 2024, 2025...），右侧是“月份”滚轮（1月, 2月...）。
*   **技术实现**：可以使用两个并排的 SwiftUI `Picker`（带有 `.pickerStyle(.wheel)`），或者使用 `UIViewRepresentable` 包装一个 `UIPickerView`。这会比在一张大日历上点选要直观得多。

---

### 2. 丰富财务分类占比（多维度的环形图）

目前的环形图只有“项目支出占比”，我们需要加入“分类占比”。建议在环形图上方增加一个 **Segmented Control（分段选择器）**，提供三个维度：

1.  **「按项目」**：维持现状，看钱都花在哪个项目了（日常、旅游、装修等）。
2.  **「全部分类」**：打通所有项目，看整体的吃、穿、住、行占比。这能让用户知道他作为一个人，整体的消费结构。
3.  **「项目内分类」**：当用户选择这个 Tab 时，下方出现一个下拉菜单，让用户选择具体的某个项目（例如选“日本游”），然后环形图立刻刷新，显示**该特定项目内的分类占比**（机票 40%、酒店 30%、餐饮 20%）。

---

### 3. 日期维度的全面升级（包含自定义周期）

这是拉开竞品差距的核心功能（很多主流记账 App 的自定义周期藏得很深或不支持）。

**优化方案**：
在页面顶部（Header 下方）加入一个**时间维度切换栏**：
`[ 日 ] [ 周 ] [ 月 ] [ 年 ] [ 自定义 ]`

*   **常规维度**：点击“月”，中间显示“2026年5月”，左右带箭头可以快速切上个月/下个月。
*   **自定义周期**：
    *   点击后弹出一个包含**两个日期选择器**的面板：“开始日期”与“结束日期”。
    *   **进阶体验设计**：由于很多用户是“发薪日记账法”（比如每月 15 号发工资，到下个月 14 号为一个周期），可以在自定义面板提供**快捷预设**，例如：“按发薪日周期 (15号 - 次月14号)”。一旦选中，系统以后默认按这个周期出报表。

---

### 4. 收支趋势图的自适应

趋势图（折线图）必须跟着上方的“时间维度”联动：
*   如果选了**「年」**：X 轴显示 12 个月（1月, 2月... 12月），每个点代表该月总和。
*   如果选了**「月」**：X 轴显示 4-5 周（第一周, 第二周...）或者按日期（1号, 5号... 30号）。
*   如果选了**「自定义」**：根据天数长短，智能决定 X 轴是按天还是按周展示。

---

### 5. 统计 Tab vs 项目详情页：报表该放哪？

这是产品架构的核心问题。我的强烈建议是：**两者都要有，但侧重点不同。**

目前我看过你的代码，`ProjectDetailView.swift`（项目详情页）里只有：概览、总收支、预算进度、账单时间轴。**它缺少了项目内的图表分析**。

#### **方案架构设计：**

**A. 全局统计 Tab (`AnalyticsView`) —— 宏观**
*   **定位**：上帝视角，看个人的财务大盘。
*   **功能**：包含上述所有的多维度切换（项目占比、全部分类占比）、包含所有项目的整体趋势图、包含所有项目总和的预算健康度。
*   **豚言豚语**：给出全局的 AI 财务建议（“你这个月整体在餐饮上超支了”）。

**B. 项目详情页 (`ProjectDetailView`) —— 微观**
*   **定位**：针对单体项目的深度复盘（比如“2024西藏行”复盘）。
*   **新增设计**：
    *   在时间轴上方，插入一个**属于该项目的环形图**（直接显示该项目下的分类占比，不需要切换维度）。
    *   插入一个**属于该项目的趋势图**（例如这个装修项目，近几个月的资金投入趋势）。
*   **好处**：用户点进“装修”项目，一眼就能看到“大头花在硬装还是软装”，点进“宠物”项目，一眼就能看到“猫粮和看病的比例”。这就是“项目制记账”最迷人的地方！

### 总结下一步的行动建议

1.  **改写 `AnalyticsView` 的头部**：引入一个 Enum `TimeRange` (day, week, month, year, custom)，根据选择动态过滤 `store.recentTransactions`。
2.  **重写 `MonthPickerSheet`**：弃用 `.graphical` 的 DatePicker，改用 `Picker` 的 `.wheel` 样式做成“年-月”双滚轮，或者“自定义区间”选择器。
3.  **升级 `DonutChartView` 上方控制区**：加入 `Picker` 切换“按项目 / 按分类”。
4.  **丰富 `ProjectDetailView`**：把 `AnalyticsView` 里的 `DonutChartView` 组件复用过去，专门喂给它当前 `project.transactions` 的分类汇总数据。

如果你同意这个设计方向，我们可以先从**替换月份选择器**或**给全局统计加上分类/项目切换**开始改造**开始！**