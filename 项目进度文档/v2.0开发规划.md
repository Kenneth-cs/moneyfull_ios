# v2.0 开发规划 & 信任基石建设

> 最后更新：2026-05-21

---

## 一、核心理念：信任是记账 App 的生死线

记账 App 的用户最敏感的不是功能多寡，而是**「数据会不会丢」**。用户每天花时间记录的每一笔账，都是个人财务隐私和时间投入。一旦数据丢失，用户会立刻卸载且不会回来。

**v2.0 的核心目标：让用户无条件信任这个 App 的数据安全性。**

---

## 二、信任基石 — P0 优先级

### 1. 数据本地存储保障（SwiftData）

**现状**：SwiftData 存储已实现，但缺少异常保护。

- [ ] **数据完整性校验**：App 启动时校验 SwiftData 容器是否正常加载，异常时展示恢复引导而非白屏崩溃
- [ ] **写入确认机制**：`addTransaction()` / `addProject()` 写入后做一次 `save()` 确保立即落盘，而非依赖 SwiftData 自动保存时机
- [ ] **数据库健康检查**：ProfileView 新增「数据状态」入口，展示当前数据条数、最后写入时间、存储占用空间

### 2. iCloud 云端备份（基础版）

> 虽然原定「达到信心线再做 iCloud」，但对记账用户来说，**「换手机/丢手机数据丢失」是第一顾虑**。基础备份不等于实时同步，成本低但信任价值极高。

- [ ] **iCloud 备份方案选型**：使用 `NSUbiquitousKeyValueStore`（轻量键值对）或 CloudKit（结构化数据）实现备份
  - 推荐方案：**CloudKit 私有数据库** — 只备份当前用户数据，不涉及共享
  - 数据结构：将 SwiftData 的 `Project` / `Transaction` / `Category` 序列化为 CKRecord
- [ ] **自动备份逻辑**：
  - 每次 App 进入后台时自动触发增量备份
  - 备份频率：每天最多 1 次完整备份 + 多次增量
  - 备份内容：所有 Project + Transaction + Category 数据
- [ ] **手动备份入口**：ProfileView 新增「立即备份」按钮，展示上次备份时间
- [ ] **恢复流程**：
  - 新设备首次安装时检测 iCloud 备份，弹窗引导恢复
  - ProfileView 新增「从 iCloud 恢复」入口（需二次确认弹窗）
  - 恢复前预览：显示备份时间和数据条数，让用户确认再恢复
- [ ] **备份状态展示**：ProfileView 显示「上次备份：今天 14:30」「备份大小：2.3 MB」


---

## 三、P1 — 商业化闭环

### 4. 分类持久化

- [ ] `AppStore` 实现 `seedDefaultCategories()`：首次启动时将 11 个系统预设类目写入 SwiftData（餐饮、交通、购物、娱乐、住房、医疗、教育、通讯、服饰、日用、其他）
- [ ] `AppStore` 实现 `fetchCategories()`：从数据库读取所有 Category
- [ ] `AddRecordView` 分类列表改为从 `store.categories` 读取，移除底部硬编码 `categories` 数组

### 5. StoreKit 2 IAP

- [ ] 新建 `StoreManager`：StoreKit 2 实现 Pro 买断（¥38）购买 + 恢复购买
- [ ] 注入为 EnvironmentObject，全局可访问 `storeManager.isPro`
- [ ] 实现 `ProUpgradeView` 付费墙 UI

### 6. 免费层限制

- [ ] `AppStore.addProject()` 中检查：非 Pro 用户最多 3 个项目（不含「日常收支」）
- [ ] 超限时弹出 `ProUpgradeView`
- [ ] 免费用户可用：完整记账 + 通用类目 + 自定义类目 + 最多 3 个项目 + 基础报表

### 7. 隐私政策页

- [ ] ProfileView 加入隐私政策入口
- [ ] WebView 加载外链或本地 HTML
- [ ] App Store 审核必需

---

## 四、P1 — 功能完整性

- [ ] **交易记录编辑 / 删除**：ProjectDetailView 补充滑动删除 + 编辑 Sheet
- [ ] **数据导出 CSV**（Pro 功能）：ProfileView 导出按钮实现，遍历 transactions 生成 CSV，通过 `UIActivityViewController` 分享
- [ ] **自定义类目管理**：ProfileView 类目管理页支持新增 / 删除自定义类目（免费可用）

---

## 五、P2 — 体验打磨

- [ ] **Haptic 触觉反馈**：导航切换、记账成功、删除、大额输入（>1000）
- [ ] **深色模式全局适配**：逐视图检查渐变卡片、图表颜色，确保深色下无白底漏色
- [ ] **空状态引导**：Projects 空列表、Dashboard 无记录时展示卡皮引导文案

---

## 六、P3 — 上架准备

- [ ] **埋点补全**：`pro_upgrade_click`、`pro_purchase_success`、`project_create`、`transaction_add`（参照埋点方案文档）
- [ ] **App Store 截图**：6.7寸 + 6.5寸 + 5.5寸
- [ ] **ASO 文案**：标题「钱小满-项目记账本」，关键词「记账 项目记账 旅行记账 装修记账 约会花费」
- [ ] **TestFlight 内测**：20-30 人，收集崩溃和核心流程反馈
- [ ] **App Store Connect 配置**：Bundle ID、IAP 产品创建、Sandbox 测试账号

---

## 七、v2.0 AI Native 升级完成说明 (2026-05-21)

### 阶段一：核心架构与 AI 基础体验 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| 数据模型升级 | ✅ | Transaction 新增 source 字段，新增 ChatHistory、MemoryRule |
| LLMService | ✅ | 对接通义千问 API，支持意图识别和 OCR 解析 |
| ContextManager | ✅ | 管理分类列表、项目列表、记忆规则的上下文注入 |
| SpeechService | ✅ | 使用 SFSpeechRecognizer 实现语音转文字 |
| AI 对话页面 | ✅ | Chat UI、记账确认卡片、新分类创建、记忆捕获 |
| TabBar 重构 | ✅ | 底部中间替换为 AI 助手按钮，支持长按录音 |

### 阶段二：视觉解析与系统级整合 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| VisionService | ✅ | 使用 iOS Vision 框架提取图片文本 |
| OCR 解析 | ✅ | LLMService 支持将 OCR 文本发给 LLM 解析 |
| App Intents | ✅ | RecordTransactionIntent 支持 Siri 和快捷指令 |
| 快捷指令 OCR | ✅ | 截屏 → 提取文本 → 钱小满自动记账 |
| Deep Link | ✅ | moneyfull://ai?text=... 处理 |

### 阶段三：传统 GUI 优化与自动化 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| 智能推荐分类 | ✅ | 根据时间/场景动态推荐 3 个常用分类 |
| 键盘交互优化 | ✅ | 滑动页面自动收起自定义键盘 |
| 周期账单模型 | ✅ | 新增 RecurringBill 数据模型 |
| AI 记忆管理 | ✅ | 用户可查看/删除 AI 学习到的纠错规则 |
| LLM 财务建议 | ✅ | 深度报告按钮接入 AI 分析 |
| 语音记账 | ✅ | 长按底部 AI 按钮录音，松手自动识别 |
| 日期格式优化 | ✅ | 纯数字格式，修复昨天显示今天的问题 |

### 快捷指令方案

最终采用 **App Intent + URL Scheme 桥梁方案**：
- 快捷指令配置：截屏 → 提取文本 → 钱小满（内容：图像中的文本）
- Intent 内部调用 `openURL` 唤起 App，不碰数据库，彻底解决 CloudKit 崩溃
- App 被唤起后通过 `.onOpenURL` 处理，弹出 AI 对话页

### 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| CategorySelectionView.swift | 修改 | 添加智能推荐逻辑和 UI |
| AddRecordView.swift | 修改 | 优化键盘收起手势 |
| Models.swift | 修改 | 新增 RecurringBill 模型 |
| moneyfull_iosApp.swift | 修改 | 注册 RecurringBill 到 ModelContainer |
| MemoryManagementView.swift | 新增 | AI 记忆管理页面 |
| ProfileView.swift | 修改 | 添加记忆管理入口 |
| LLMService.swift | 修改 | 新增 generateFinancialAdvice 方法 |
| AnalyticsView.swift | 修改 | 集成 LLM 财务建议 |
| MainTabView.swift | 修改 | 语音录音、Deep Link 监听 |
| AIChatView.swift | 修改 | initialText、isFromShortcut、processOCRText |
| ContentView.swift | 修改 | URL Scheme Deep Link 处理 |
| RecordTransactionIntent.swift | 修改 | App Intent 桥梁方案 |
| BackTapTutorialView.swift | 修改 | 更新快捷指令教程 |
| DashboardView.swift | 修改 | 日期格式优化 |
| ProjectsView.swift | 修改 | 日期格式优化 |
| SpeechService.swift | 新增 | 语音转文字服务 |
| VisionService.swift | 新增 | 图片 OCR 服务 |
| LLMService.swift | 新增 | LLM API 服务 |
| ContextManager.swift | 新增 | 上下文管理服务 |
