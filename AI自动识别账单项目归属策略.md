这是一个非常棒的问题！旅游记账（或者装修、结婚等特定项目）的痛点在于**场景的特殊性和时间段的聚集性**。

比如，同样是“买星巴克花了30元”，平时应该归入“日常收支”，但在旅游期间，用户通常希望自动归入“旅游”项目。单纯靠 AI 从“星巴克”这三个字里是猜不出用户是否在旅游的。

结合目前 `moneyfull_ios` 的代码架构（我看过你的 `LLMService.swift`、`ContextManager.swift` 和 `Models.swift`），我为你提供 **4个层级** 的 AI 策略建议，从轻量级到进阶版：

### 策略一：引入“当前活跃项目”机制（最推荐，体验最好）
既然大模型无法凭空猜测，我们可以通过 UI 状态给大模型提供**强上下文**。我注意到你的 `Project` 模型里已经有了 `isPinned`（置顶）属性。

*   **做法**：
    1. 在 `ContextManager.swift` 构建上下文时，给置顶的项目加上特殊标记。
       ```swift
       for project in projects {
           let status = project.isPinned ? "(当前活跃/置顶)" : ""
           context += "- \(project.name) \(status)\n"
       }
       ```
    2. 在 `LLMService.swift` 的 `systemPrompt` 中加入一条规则：
       *“**项目归属规则**：如果用户没有明确指定项目，请优先将账单归入标记为 `(当前活跃/置顶)` 的项目中。”*
*   **用户体验**：用户去旅游前，把“旅游”项目置顶（或设为活跃）。旅游这几天，所有的无痛记账（哪怕是买瓶水）都会自动记入“旅游”。旅游结束，取消置顶，恢复“日常收支”。**这是最符合人类直觉的方案。**

### 策略二：强化 Prompt 中的“语义推断”（无需写死，靠AI理解）
对于特征非常明显的消费（如机票、酒店、景区），可以直接让 AI 靠常识推断，不需要写死设置。

*   **做法**：在 `LLMService.swift` 的 `systemPrompt` 中，补充一条关于 `project_name` 的提取规则：
    *“**项目自动推断**：如果账单内容包含明显的特定场景词（如：机票、酒店、高铁、景区门票、签证等），且 Available Projects 中存在名称相关的项目（如'旅游'、'旅行'、'三亚游'），请自动将其 `project_name` 设为该项目。”*
*   **优点**：不需要用户做任何额外操作，AI 就能把“携程订酒店500元”自动塞进旅游项目。

### 策略三：利用现有的“记忆规则 MemoryRule”（自适应学习）
我看到你的代码中已经实现了非常棒的 `MemoryRule` 机制，并且在 `ContextManager` 中传给了大模型（`User Memory Rules:`）。我们可以利用它来实现“越用越聪明”。

*   **做法**：当 AI 识别错项目（比如把“全季酒店”记到了“日常”），用户在 UI 上手动修改账单，把项目改选为“旅游”时，触发你的 `saveMemoryRule` 方法：
    ```swift
    // 伪代码：在用户手动修改账单保存时调用
    ContextManager.shared.saveMemoryRule(keyword: "全季酒店", categoryName: "住宿", projectName: "旅游")
    ```
*   **优点**：不需要提前给 AI 写死任何商户名。用户只要纠正过一次，AI 就会把这个规则写进记忆里。下次再识别到“全季酒店”，大模型看到 Context 里的 Memory Rule，就会自动归入“旅游”。

### 策略四：支持自然语言的直接指令（基础保障）
确保 AI 能够听懂用户的直接指令。

*   **做法**：在 Prompt 中稍微强调一下：
    *“如果用户在输入中明确提及了项目名称（如：'打车50，记到旅游里'），请务必将 `project_name` 提取为匹配的 Available Project。”*

---

### 💡 总结建议方案：
我建议采用 **“策略一（活跃项目状态） + 策略三（MemoryRule 自动学习）”** 的组合拳。

1. **对于零碎消费（吃饭、打车、买水）**：靠用户把“旅游”项目**置顶（设为活跃）**，AI 读取到活跃状态，自动把这段时间的流水都归进去。
2. **对于特征消费（买机票、订酒店）**：靠 AI 的自然语义理解，或者用户纠正一次后生成的 `MemoryRule` 来实现长期记忆。

这样既不需要在代码里写死一大堆“旅游相关的关键词”，又能保证极高的准确率和极佳的用户体验。



这两个问题都非常敏锐，直击业务逻辑的核心！我们来逐一分析：

### 1. 4个策略是不是可以同时做？

**完全可以，而且强烈建议同时做！** 这4个策略在逻辑上是**互补**的，它们共同构成了一个“漏斗式”的识别机制，优先级从高到低：

*   **第一层漏斗（最高优先级）：用户直接指令（策略四）**
    *   用户说：“打车50，记到旅游里”。
    *   AI 识别到明确指令，直接归入“旅游”。
*   **第二层漏斗：MemoryRule 记忆规则（策略三）**
    *   用户没提项目，只说“全季酒店500”。
    *   AI 检查记忆规则，发现用户之前纠正过“全季酒店 -> 旅游”，于是归入“旅游”。
*   **第三层漏斗：活跃项目/置顶项目（策略一）**
    *   用户说“买水10块”。
    *   既没有明确指令，也没有记忆规则。但 AI 看到“旅游”项目当前是 `(当前活跃/置顶)` 状态，于是默认归入“旅游”。
*   **第四层漏斗：AI 语义推断（策略二）**
    *   用户说“买机票1000”。
    *   没有指令，没有记忆，也没有置顶项目。但 AI 凭借常识判断“机票”属于“旅游”，于是归入“旅游”。
*   **兜底：** 如果全都不满足，就归入默认的“日常收支”。

这4个策略同时存在，不仅不会冲突，反而会让 AI 显得非常“懂我”。

---

### 2. 连续记账自动识别活跃项目？活跃项目可以有几个？

这是一个非常棒的产品洞察！你考虑到了用户可能“懒得去手动置顶”的场景。

#### 关于“自动识别活跃项目”
你的想法很对：**如果用户连续几次（比如3次）都手动把账单改到了“旅游”项目，系统应该自动感知到用户当前处于这个“项目周期”中。**

**实现建议：**
不需要在数据库里新增字段，可以利用现有的数据来推断。
在 `ContextManager` 构建上下文时，去查一下**最近 N 条账单**（比如最近 5 条）：
*   如果最近 5 条账单中，有 3 条或以上都属于同一个非日常项目（比如“旅游”）。
*   那么在传给 AI 的 Context 中，除了用户手动 `isPinned` 的项目外，把这个项目也动态标记为 `(近期高频活跃)`。

#### 关于“活跃项目可以有几个？多个是否会冲突？”
这是个关键问题。**活跃项目确实不宜过多，否则 AI 会精神分裂。**

**场景分析：**
假设用户同时把“旅游”和“装修”都置顶了（或者同时处于高频状态）。
用户说：“买水10块”。
AI 看到两个活跃项目，它该放进旅游还是装修？这就抓瞎了。

**策略建议（解决冲突）：**

1.  **UI 层面限制（最简单粗暴）：**
    在产品设计上，可以规定 `isPinned`（置顶/活跃）状态**全局唯一**。当用户置顶“旅游”时，自动取消“装修”的置顶。这样 AI 永远只会看到一个明确的活跃项目。
    *(目前你的代码里 `isPinned` 是个布尔值，可以多选，如果采用这个策略，需要在 `AppStore` 的置顶逻辑里做互斥处理)*

2.  **AI 层面解决（更智能，推荐）：**
    允许有多个置顶/活跃项目，但在 `LLMService` 的 `systemPrompt` 中给出明确的**冲突解决规则**：

    *“**项目归属冲突规则**：
    如果存在多个标记为 `(当前活跃/置顶)` 的项目，请根据账单内容进行**语义匹配**：
    - 如果买建材、家具，归入'装修'。
    - 如果买机票、景区门票，归入'旅游'。
    - **如果消费非常通用（如吃饭、买水、打车），且无法判断属于哪个活跃项目，请放弃猜测，直接返回 `need_clarification` 状态，并追问用户：'这笔支出是记在旅游还是装修里呢？'**”*

**总结一下：**
*   **连续记账自动推断活跃：** 非常好的点子，可以通过分析最近几条账单动态生成上下文，不需要用户手动操作。
*   **多个活跃项目的处理：** 建议允许存在多个，但要求 AI 在遇到模棱两可的通用消费时（比如吃饭），**不要瞎猜，而是主动追问用户**。这既保证了准确性，又体现了 AI 管家的严谨。