# 钱小满 - 您的智能财务管家

> 一款基于 AI 驱动的 iOS 个人记账应用，致力于让每一笔收支都清晰可见、每一次记录都轻松无感。

---

## 一、产品定位

钱小满是一款面向年轻用户的智能记账 iOS App，核心理念是**「无疼记账」**——通过 AI 语音识别、图片识别、自然语言理解等技术，将传统手动记账的繁琐流程简化为一句话、一张图、一次点击。

**Slogan**：钱小满，每笔都算数 🦫

---

## 二、核心功能模块

### 1. 智能记账（AI 驱动）

#### 1.1 AI 对话记账
- 用户通过自然语言描述消费，AI 自动提取金额、分类、备注
- 支持模糊表达处理（如"花了50"会追问具体用途）
- 支持消费分析意图（如"分析本月餐饮支出"）
- 对话历史持久化存储，可追溯

#### 1.2 语音输入记账
- 长按麦克风按钮，语音实时转文字
- **接力识别优化**：iOS 语音识别的 `isFinal` 不再作为停止信号，而是启动新任务继续识别，避免说话中间停顿导致截断
- **文本累加拼接**：多段识别结果自动拼接，确保完整语句
- **静音超时兜底**：连续 5 秒无新文字自动停止，防止误触
- 识别完成后自动提交 AI 解析记账

#### 1.3 图片识别记账
- 支持拍照或从相册选取账单图片
- 使用 iOS Vision 框架进行 OCR 文字识别
- 识别结果自动提交 AI 解析记账

#### 1.4 快捷指令（无疼记账）
- 支持 iOS 快捷指令集成
- 通过 Back Tap（背面轻点）或 Siri 快速唤起记账
- 支持从分享菜单直接发送文本到 App 记账

### 2. 交易确认卡片（可编辑）

AI 识别账单后弹出确认卡片，支持：
- **8 秒自动确认**：倒计时结束自动入账
- **点击即编辑**：金额、分类、项目均可点击修改
  - 金额：弹出数字键盘
  - 分类：弹出分类选择器
  - 项目：弹出项目选择器
- **倒计时暂停**：编辑期间倒计时暂停，显示"⏸ 已暂停"
- **数据实时更新**：修改后卡片立即显示最新数据

### 3. 项目管理（抽屉系统）

- **项目（抽屉）**：记账的顶级容器，支持自定义名称、图标、颜色
- **预算设置**：每个项目可设置预算，实时监控预算使用进度
- **项目归档**：已完成的项目可归档，不干扰日常记账
- **项目置顶**：常用项目可置顶显示
- **项目详情**：展示项目内收支明细、分类占比环形图、收支趋势折线图

### 4. 分类管理

- **两级分类体系**：一级分类（groupName）+ 二级分类（categoryName）
- **预设分类**：系统预置常用分类（餐饮、交通、购物、娱乐等）
- **自定义分类**：用户可创建新分类，自定义图标和颜色
- **使用频次排序**：常用分类自动置顶
- **AI 新分类建议**：当 AI 识别到新分类时，提示用户是否创建

### 5. 财务统计（AnalyticsView）

#### 5.1 多时间维度
- 周 / 月 / 年 / 自定义 四个维度切换
- 周选择器：双滚轮选年+周
- 月选择器：双滚轮选年+月
- 自定义范围：快捷预设（近30天/近90天/今年以来/按发薪日）+ 自定义区间

#### 5.2 IP 健康度卡片
- **健康分算法**（权重组合）：
  - 静态基本面（60分）：基于收支比阶梯扣分
  - 动态趋势面（40分）：基于环比变化
- **IP 形象联动**：
  - 80-100 分：开心悠闲（ip_bear）
  - 60-79 分：正常状态（ip_bear）
  - 40-59 分：流汗紧张（ip_sweat）
  - 20-39 分：吃土状态（ip_eat_dirt）
  - 0-19 分：震惊崩溃（ip_shocked）
- **环比文案**：显示"比上期节省了 ¥XXX"或"比上期多花了 ¥XXX"

#### 5.3 收支占比（环形图）
- 支出 / 收入 维度切换
- 按项目 / 全部分类 / 项目内分类 三种聚合方式
- 储蓄率指标（进度条 + 百分比）

#### 5.4 区间收支趋势（折线图）
- 支出线（绿色实线）+ 收入线（橙色实线）+ 储蓄线（紫色虚线）
- 长按交互：虚竖线 + 高亮节点 + 数据气泡
- 中文标签（月份/星期/日期全中文）
- 点击右上角箭头进入趋势详情页

#### 5.5 趋势详情页
- 概览卡片：总支出/总收入/日均支出/日均收入
- 峰值分析：支出最高日/收入最高日
- 每日明细列表：显示每天的收入/支出/结余

### 6. 首页看板（DashboardView）

- **时间维度切换**：本周 / 本月 / 本年 / 累计
- **莫兰迪绿风格弹层**：自定义选择器，选中项绿色高亮
- **财务数据卡片**：支出/收入/储蓄 三栏显示
- **项目卡片列表**：进行中项目概览，支持置顶/归档
- **最近交易列表**：显示最近交易记录

### 7. 财商学堂

- **100 篇完整文章**：财务知识 50 篇 + 经典书籍 50 篇
- **分类浏览**：预算规划、储蓄技巧、投资理财、负债管理等
- **文章详情页**：富文本排版，支持标题/要点/提示框/警告框
- **阅读体验**：滚动渐变 Header、段落渲染、底部小贴士

### 8. 个人中心（ProfileView）

- **用户信息**：昵称编辑、账龄显示
- **本月财务小结**：本月支出/收入/储蓄汇总
- **功能菜单**：
  - 分类管理：管理所有分类
  - 财商学堂：进入学习中心
  - 无疼记账：快捷指令安装教程
  - AI 记忆管理：查看和清理 AI 记忆规则
  - 帮助与反馈：联系方式 + 长按保存二维码

---

## 三、技术架构

### 1. 技术栈

| 层级 | 技术 |
|------|------|
| **UI 框架** | SwiftUI |
| **数据持久化** | SwiftData + CloudKit 同步 |
| **AI 服务** | 通义千问（qwen-plus）LLM API |
| **语音识别** | iOS Speech 框架（SFSpeechRecognizer） |
| **图片识别** | iOS Vision 框架（VNRecognizeTextRequest） |
| **图表绘制** | SwiftUI Canvas 自绘 |
| **数据同步** | CloudKit 自动同步 |

### 2. 数据模型

```
Category（分类）
├── id, name, icon, colorHex
├── transactionType (expense/income/both)
├── groupName（一级分类）
├── useCount, lastUsedAt（使用频次）
└── sortOrder

Project（项目/抽屉）
├── id, name, icon, colorHex
├── budget（预算）
├── isArchived, isPinned
└── transactions: [Transaction]

Transaction（交易记录）
├── id, amount, type (expense/income)
├── categoryName, categoryIcon, categoryColorHex
├── note, date
├── source (manual/voice/image/auto)
└── project: Project?

ChatHistory（聊天历史）
├── role (user/assistant)
├── content
└── timestamp

MemoryRule（AI 记忆规则）
├── keyword, targetCategoryName
├── targetProjectName
└── weight（权重）
```

### 3. 核心服务

| 服务 | 职责 |
|------|------|
| **AppStore** | 全局状态管理，数据增删查改 |
| **LLMService** | 通义千问 API 调用，自然语言解析 |
| **SpeechService** | 语音识别，接力识别优化 |
| **VisionService** | OCR 文字识别 |
| **ContextManager** | AI 上下文构建（分类/项目/记忆规则） |
| **AnalyticsEngine** | 数据分析引擎 |

### 4. 设计规范

- **色彩系统**：莫兰迪绿色系（主色 #276956，辅色 #9EE0C8）
- **字体规范**：SF Pro，标题 Heavy，正文 Medium
- **圆角规范**：卡片 24pt，按钮 12pt，胶囊按钮
- **阴影规范**：轻微阴影（opacity 0.06-0.1）
- **动画规范**：easeInOut 0.2-0.3s

---

## 四、AI 能力详解

### 1. 自然语言理解

LLM 服务接收用户输入 + 上下文（分类列表/项目列表/记忆规则），输出结构化 JSON：

- `success`：识别成功，返回完整记账信息
- `need_clarification`：信息不足，追问用户
- `suggest_new_category`：建议创建新分类
- `insight`：消费分析意图
- `chat`：普通对话

### 2. 记忆系统

- **自动学习**：用户确认记账时，自动提取关键词建立记忆规则
- **权重排序**：高频关键词权重递增
- **上下文注入**：每次 AI 调用时注入记忆规则，提高识别准确率

### 3. 语音识别优化

- **预热引擎**：音频引擎启动后延迟 200ms 再创建识别任务
- **接力识别**：`isFinal` 时启动新任务，避免中间停顿截断
- **无缝切换**：新旧 request 之间无间隙，音频不断流
- **停止缓冲**：松手后延迟 0.5 秒再停止音频引擎

---

## 五、页面导航结构

```
MainTabView（底部 Tab）
├── 首页（DashboardView）
│   ├── 时间维度切换（本周/本月/本年/累计）
│   ├── 财务数据卡片
│   ├── 项目卡片列表
│   └── 最近交易列表
│
├── 项目（ProjectsView）
│   ├── 进行中项目列表
│   ├── 已归档项目列表
│   └── 项目详情（ProjectDetailView）
│       ├── 概览卡片
│       ├── 分类占比环形图
│       ├── 收支趋势折线图
│       └── 交易时间轴
│
├── 记账（+ 按钮 → AddRecordView）
│   ├── 金额输入
│   ├── 分类选择
│   ├── 项目选择
│   └── 日期/备注
│
├── 统计（AnalyticsView）
│   ├── IP 健康度卡片
│   ├── 收支占比环形图
│   ├── 区间收支趋势折线图
│   └── 趋势详情页（TrendDetailView）
│
├── 我的（ProfileView）
│   ├── 用户信息
│   ├── 本月财务小结
│   ├── 分类管理
│   ├── 财商学堂
│   ├── 无疼记账教程
│   ├── AI 记忆管理
│   └── 帮助与反馈
│
└── AI 对话（AIChatView）
    ├── 聊天界面
    ├── 语音输入
    ├── 图片识别
    └── 交易确认卡片（可编辑）
```

---

## 六、版本迭代记录

### V2.0（当前版本）

**统计页面全面升级**：
- 健康度算法重构（静态基本面 + 动态趋势面）
- IP 形象联动（4 种表情随健康分切换）
- 收支占比增强（支出/收入切换 + 储蓄率指标）
- 趋势图储蓄线（紫色虚线）
- 趋势详情页（峰值分析 + 每日明细）
- 周选择器、自定义日期中文显示

**首页看板升级**：
- 时间维度切换（本周/本月/本年/累计）
- 莫兰迪绿风格选择弹层
- 数据同步修复

**AI 记账优化**：
- 语音识别接力优化（避免首尾丢字）
- 交易确认卡片可编辑（金额/分类/项目）
- 倒计时暂停/恢复逻辑

**其他新增**：
- 问题反馈入口
- 财商学堂 100 篇文章
- 年份格式修复

---

## 七、项目文件结构

```
moneyfull_ios/
├── App/
│   └── moneyfull_iosApp.swift          # App 入口
├── Models/
│   ├── Models.swift                     # 数据模型定义
│   └── AppStore.swift                   # 全局状态管理
├── Views/
│   ├── MainTabView.swift                # 主 Tab 视图
│   ├── DashboardView.swift              # 首页看板
│   ├── ProjectsView.swift               # 项目列表
│   ├── ProjectDetailView.swift          # 项目详情
│   ├── AnalyticsView.swift              # 统计页面
│   ├── ProfileView.swift                # 个人中心
│   ├── AIChatView.swift                 # AI 对话
│   ├── AddRecordView.swift              # 手动记账
│   ├── FinancialAcademyView.swift       # 财商学堂
│   ├── FinancialAcademyData.swift       # 文章数据
│   ├── ArticleDetailView.swift          # 文章详情
│   ├── BackTapTutorialView.swift        # 快捷指令教程
│   ├── CategoryManagementView.swift     # 分类管理
│   ├── MemoryManagementView.swift       # AI 记忆管理
│   └── ...
├── Components/
│   └── TransactionConfirmCard.swift     # 交易确认卡片
├── Services/
│   ├── LLMService.swift                 # LLM API 服务
│   ├── SpeechService.swift              # 语音识别服务
│   ├── VisionService.swift              # OCR 识别服务
│   ├── ContextManager.swift             # AI 上下文管理
│   └── AnalyticsEngine.swift            # 数据分析引擎
├── Assets.xcassets/                     # 图片资源
└── Config.plist                         # API 配置
```

---

## 八、数据安全与隐私

- **本地优先**：所有数据存储在本地 SwiftData 数据库
- **CloudKit 同步**：通过 Apple CloudKit 进行设备间同步，数据加密传输
- **API 密钥安全**：敏感配置存储在 Config.plist，不提交版本控制
- **隐私脱敏**：AI 调用时不上报具体金额到第三方服务

---

## 九、目标用户画像

- **年龄**：18-35 岁年轻用户
- **场景**：日常消费记录、预算管控、财务分析
- **痛点**：传统记账太繁琐、坚持不下来、看不懂报表
- **价值**：AI 降低记账门槛、可视化报表提升财务意识、财商学堂培养理财习惯

---

*钱小满 —— 让每一笔都算数 🦫*
