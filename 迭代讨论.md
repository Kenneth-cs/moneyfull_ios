这三个问题非常深入，涉及到了 AI Native 应用的核心架构设计。以下是我的详细解答：

### 1. 关于 AI 自动创建个性化分类的“度”

**问题：** 强制 AI 只能在现有分类里映射是不是太死板了？如果允许 AI 自动创建分类，又怕太散乱，怎么把握度？

**解答：**
您说得对，完全限制死会失去 AI 的灵活性，但完全放开会导致分类灾难（比如 AI 可能会建出“星巴克”、“瑞幸”、“买咖啡”三个不同的分类，导致报表完全没法看）。

**把握“度”的策略（Human-in-the-loop 人机协同）：**

*   **原则：AI 只有“建议权”，没有“直接创建权”。**
*   **具体流程：**
    1.  **优先映射：** AI 拿到“喝了一杯瑞幸 20块”，首先在用户现有的分类（比如只有“餐饮”）中寻找。
    2.  **触发阈值：** 如果 AI 发现现有的分类都不太合适，或者它认为“咖啡”是一个高频且独立的消费场景，它会在生成的“记账确认卡片”上，将分类标为 **“✨ 咖啡 (新)”**。
    3.  **用户确认：** 这个“新分类”在用户点击“确认入账”之前，是不存在的。
        *   如果用户觉得没必要，点击卡片把它改回“餐饮”，AI 学习到：*此用户不需要咖啡分类*。
        *   如果用户点击“确认”，系统才真正在底层数据库创建一个名为“咖啡”的新分类，并分配一个默认的 Icon 和颜色。
*   **总结：** 通过这种方式，AI 展现了它的聪明（能识别出新场景），但最终的分类体系依然牢牢掌控在用户手里，避免了数据污染。

### 2. 几百轮对话后，Context 和记忆数据庞大怎么处理？

**问题：** 每次对话都带上记忆，数据量大了怎么办？

**解答：**
这是所有长文本/长期记忆 AI 应用都会面临的 Context Window（上下文窗口）爆炸问题。我们不能把所有历史对话都塞进 Prompt 里。

**处理策略（分层记忆管理）：**

*   **短期记忆（滑动窗口 Sliding Window）：**
    *   **处理方式：** 每次请求 LLM 时，只携带**最近 N 轮**（比如最近 5 轮）的对话历史。
    *   **目的：** 解决多轮追问（如：User: "花了50" -> AI: "花在哪了？" -> User: "打车"）。超过 5 轮的闲聊直接丢弃，不占用 Token。
*   **长期记忆（向量化与规则提取）：**
    *   **处理方式：** 用户的历史纠错和偏好，绝对**不能**以原始对话的形式保存。我们需要一个后台的“记忆整理机制”。
    *   **机制：** 当用户纠错（把“瑞幸”改为“咖啡”）时，App 在本地生成一条结构化的 JSON 规则：`{"keyword": "瑞幸", "category": "咖啡", "weight": 1}`。
    *   **注入方式：** 每次请求 LLM 时，只把这些**精简的规则 JSON** 注入到 System Prompt 中。比如：“用户偏好规则：遇到'瑞幸'归入'咖啡'”。这只会占用几十个 Token。
*   **总结：** 传给 LLM 的 Context = `System Prompt (人设)` + `精简的用户规则 JSON (长期记忆)` + `当前分类列表` + `最近 5 轮对话 (短期记忆)`。这样即使聊了一万句，每次请求的 Token 数量依然是固定且极小的。

### 3. Agent 数据和记忆存在哪里？需要改技术架构和数据库吗？

**问题：** Agent 的信息、记忆存哪里？要不要上服务器 MySQL？之前的 SwiftData 方案要不要变？

**解答：**
**好消息是：完全不需要改变现有的技术架构，也不需要强制上服务器 MySQL。**

苹果生态的哲学是 **“On-Device Intelligence（端侧智能）”和“Privacy First（隐私优先）”**。我们完全可以把 Agent 的记忆和状态存在本地，并通过 iCloud 同步。

**具体存储方案（基于现有的 SwiftData）：**

1.  **不需要变的基础数据：**
    *   您现有的 `Transaction`, `Project`, `Category` 模型完全保持不变，继续存在 SwiftData 里。
2.  **新增的 Agent 数据模型（存在本地 SwiftData）：**
    *   **新增 `ChatHistory` 模型：** 用于存储 AI 对话页面的聊天记录（文本、时间、发送者是 User 还是 AI）。只存最近的几百条即可，太老的可以在本地清理。
    *   **新增 `UserPreference` (或 `MemoryRule`) 模型：** 用于存储刚才提到的纠错规则。
        ```swift
        @Model
        class MemoryRule {
            var keyword: String      // 例如 "瑞幸"
            var targetCategoryName: String // 例如 "咖啡"
            var createdAt: Date
            // ...
        }
        ```
3.  **为什么不需要 MySQL？**
    *   **成本与隐私：** 记账是非常私密的数据。如果把用户的消费习惯、纠错规则传到您的 MySQL 服务器，不仅增加了您的服务器成本，还可能引发用户的隐私担忧。
    *   **iCloud 足够强大：** 现有的 `ModelConfiguration(cloudKitDatabase: .automatic)` 已经能完美地把这些新增的 `ChatHistory` 和 `MemoryRule` 同步到用户的各个苹果设备上。
    *   **无网可用：** 存在本地意味着即使用户在地铁里没网，App 依然能打开，只是无法调用云端 LLM，但历史数据和规则都在。

**总结架构：**
*   **UI 层：** SwiftUI
*   **数据层：** SwiftData + CloudKit (本地存储 + 苹果官方云同步) —— **保持现状，只新增模型表。**
*   **AI 层：** 手机端组装 Prompt (包含本地查出来的规则) -> 发送网络请求给云端大模型 API (如 DeepSeek/千问) -> 拿到 JSON 结果 -> 写入本地 SwiftData。

这样设计，既实现了强大的 Agent 能力，又保持了架构的轻量、免费（无服务器成本）和极致的隐私保护。